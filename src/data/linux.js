/* Linux Topic Data — CloudTruck */

export const LINUX_COMPONENTS = {
  'process': {
    group: 'processes-memory',
    name: 'Processes & PIDs',
    short: 'A running program with its own memory, files, and identity',
    color: '#2563eb',
    tag: 'PROCESS',
    intro: 'A process is a running instance of a program: its own virtual address space, a set of open file descriptors, an environment, a current directory, and a unique process ID (PID). Every process except the very first has a parent, forming a tree rooted at PID 1 (systemd). New processes are born by fork() — which clones the caller — usually followed immediately by exec(), which replaces the clone\'s memory image with a new program. This fork/exec pair is how every command in your shell actually starts.',
    deepDive: 'Process state is the key to reading the system. A process is Running, Sleeping (waiting on I/O or an event), Stopped, or a Zombie. A zombie (state Z) is a process that has exited but whose parent has not yet called wait() to read its exit status — it holds nothing but a PID slot, and a pile of them means a buggy parent that is not reaping children. An orphan (parent died first) gets re-parented to PID 1, which reaps it. Each process has a real and effective UID/GID that drive permission checks, and file descriptors 0/1/2 wired to stdin/stdout/stderr. Understanding that fork() copies the descriptor table is why pipes and redirection work the way they do.',
    example: {
      scenario: 'A service spawned a runaway child and on-call needs to find it and inspect the process tree.',
      action: 'They use ps with a tree view and pstree to see parent/child relationships, then read the process\'s details from /proc.',
      code: '$ ps -ef --forest | grep -A3 checkout\nroot      812    1  0 systemd\napp       940  812  0  \\_ /usr/bin/checkout\napp      2231  940 99      \\_ /usr/bin/report --loop   # runaway\n\n$ pstree -p 940\ncheckout(940)───report(2231)\n\n# Inspect the process from /proc\n$ cat /proc/2231/status | grep -E "State|PPid|VmRSS"\n$ ls -l /proc/2231/fd      # open file descriptors',
    },
    responsibilities: [
      'Run a program in its own isolated virtual address space',
      'Carry a unique PID and a parent PID forming the process tree',
      'Be created via fork() and replaced via exec()',
      'Hold open file descriptors, environment, and working directory',
      'Be reaped by its parent so its PID slot is freed',
    ],
    docUrl: 'https://man7.org/linux/man-pages/man7/credentials.7.html',
    docLabel: 'Linux process credentials',
  },
  'scheduler': {
    group: 'processes-memory',
    name: 'Scheduler & Priorities',
    short: 'How the kernel shares CPUs across competing processes',
    color: '#0891b2',
    tag: 'CPU',
    intro: 'There are always more runnable processes than CPUs, so the kernel scheduler decides who runs, when, and for how long. Linux uses the Completely Fair Scheduler (CFS) for normal tasks: it tracks how much CPU time each process has received and runs the one that is most "behind," creating the illusion that everyone runs simultaneously. Priority is tunable with the nice value (-20 highest, +19 lowest), and special real-time scheduling classes exist for latency-critical work. When you feel a machine is "slow," the scheduler and the run queue are where the story usually begins.',
    deepDive: 'Load average — the famous 1/5/15-minute numbers — is not CPU usage; it is the number of processes runnable or in uninterruptible sleep (D state, usually blocked on I/O). A load of 8 on an 8-core box is fully utilized; the same load on a 2-core box means heavy queuing. This is why a high load average with low CPU usage points at I/O, not compute. nice and renice adjust CPU priority; ionice adjusts I/O priority. cgroups (the same mechanism containers use) cap and weight CPU shares for whole groups of processes. The distinction between user time, system (kernel) time, and iowait in top is the fastest way to classify a slowdown as application, syscall-heavy, or disk-bound.',
    example: {
      scenario: 'A box shows load average 12 but CPU looks under 30% — on-call needs to tell CPU starvation from I/O wait.',
      action: 'They read load against core count, then break the time down with top and find processes stuck in D (uninterruptible) state.',
      code: '$ uptime\n14:22  up 9 days,  load average: 12.4, 11.8, 9.2\n$ nproc\n4                         # load 12 on 4 cores = heavy queue\n\n$ top    # look at %wa (iowait) and the S column\n%Cpu(s):  18 us,  6 sy,  0 ni, 22 id, 53 wa\n\n# Find processes blocked on I/O (D state)\n$ ps -eo pid,stat,comm | awk \'$2 ~ /D/\'\n# Lower a hog\'s priority\n$ renice +10 -p 2231',
    },
    responsibilities: [
      'Time-slice runnable processes fairly across all CPUs',
      'Honor nice values and real-time scheduling classes',
      'Expose pressure through the load average and run queue',
      'Distinguish CPU-bound work from I/O wait',
      'Enforce cgroup CPU shares and limits for process groups',
    ],
    docUrl: 'https://www.kernel.org/doc/html/latest/scheduler/sched-design-CFS.html',
    docLabel: 'CFS scheduler design',
  },
  'memory': {
    group: 'processes-memory',
    name: 'Memory & Virtual Memory',
    short: 'Per-process address spaces backed by RAM and swap',
    color: '#7c3aed',
    tag: 'MEMORY',
    intro: 'Every process sees its own private, contiguous virtual address space, even though physical RAM is shared, fragmented, and smaller. The kernel and the CPU\'s MMU translate virtual addresses to physical pages on the fly, loading pages lazily (on first access) and writing idle pages out to swap when RAM is tight. This indirection is what lets processes be isolated, lets the same library be shared across many processes, and lets a program use more address space than you have RAM. Reading memory correctly is the difference between "we need a bigger box" and "we have a leak."',
    deepDive: 'The number that confuses everyone is "free memory looks low" — Linux deliberately uses spare RAM as page cache for files, and that cache is instantly reclaimable, so available (not free) is the number that matters in free -h. RSS is the resident physical memory a process actually occupies; VSZ is the much larger virtual size it has mapped but may never touch. When memory genuinely runs out and swap is exhausted, the OOM killer picks a victim by a heuristic score and kills it — and that SIGKILL with no application log is a signature worth recognizing (check dmesg). A steadily climbing RSS that never plateaus is a leak; heavy swap-in/swap-out (thrashing) tanks performance even when "memory is available."',
    example: {
      scenario: 'A service was killed with no application error and on-call suspects the OOM killer.',
      action: 'They confirm available memory and swap with free, then check the kernel ring buffer for the OOM kill record.',
      code: '$ free -h\n              total   used   free  available\nMem:           15Gi   14Gi  210Mi      280Mi   # cache exhausted\nSwap:         2.0Gi  2.0Gi     0B            # swap full\n\n$ dmesg -T | grep -i "killed process"\n[Fri Jun  5 14:02] Out of memory: Killed process 940\n  (checkout) total-vm:9.1GB, anon-rss:8.7GB\n\n# Watch a leak grow\n$ ps -o pid,rss,comm -p 940; sleep 60; ps -o pid,rss,comm -p 940',
    },
    responsibilities: [
      'Give each process a private virtual address space',
      'Map pages lazily and translate addresses via the MMU',
      'Use spare RAM as reclaimable page cache',
      'Swap idle pages to disk under memory pressure',
      'Invoke the OOM killer when memory is truly exhausted',
    ],
    docUrl: 'https://www.kernel.org/doc/html/latest/admin-guide/mm/concepts.html',
    docLabel: 'Linux memory management',
  },
  'systemd': {
    group: 'init-services',
    name: 'systemd (PID 1)',
    short: 'The init system that boots and supervises everything',
    color: '#0d9488',
    tag: 'INIT',
    intro: 'After the kernel finishes booting it starts exactly one user-space process — PID 1 — and on most modern distributions that is systemd. As the root of the process tree, systemd is responsible for bringing the system up, starting and supervising every service, ordering startup by dependencies, and reaping orphaned processes. It replaced the old sequential SysV init scripts with a declarative, parallelized, dependency-aware model. Almost every "how do I start/stop/enable this service" task on a Linux box goes through systemctl, the systemd control client.',
    deepDive: 'systemd\'s power is dependency-based parallel startup: instead of running boot scripts one after another, it builds a dependency graph from unit declarations (Wants=, Requires=, After=, Before=) and starts everything it can concurrently. It supervises services — restarting them on crash per Restart= policy, tracking their state, and capturing their output into the journal. It also manages sockets, timers (a cron replacement), mounts, and devices as first-class units. systemd-analyze blame and critical-chain are the go-to tools for a slow boot. The controversy around systemd is real (it absorbed a lot of responsibilities), but operationally it is the layer you manage services through, so fluency with systemctl and journalctl is non-negotiable.',
    example: {
      scenario: 'A newly deployed daemon must start on boot and restart automatically if it crashes.',
      action: 'They check the unit\'s status and enablement with systemctl, then confirm the restart policy is active.',
      code: '$ systemctl status checkout.service\n● checkout.service - Checkout API\n   Loaded: loaded (/etc/systemd/system/checkout.service; enabled)\n   Active: active (running) since Fri 14:02; 3min ago\n Main PID: 940 (checkout)\n\n$ systemctl enable --now checkout.service   # start + boot-persist\n$ systemctl is-enabled checkout.service\nenabled\n\n# Why was boot slow?\n$ systemd-analyze blame | head -5',
    },
    responsibilities: [
      'Run as PID 1, the root of the entire process tree',
      'Bring the system up by activating units in dependency order',
      'Supervise services and restart them per policy',
      'Reap orphaned processes re-parented to it',
      'Provide systemctl as the single service-control interface',
    ],
    docUrl: 'https://www.freedesktop.org/software/systemd/man/latest/systemd.html',
    docLabel: 'systemd manual',
  },
  'units': {
    group: 'init-services',
    name: 'Units & Targets',
    short: 'Declarative files describing services, sockets, and boot states',
    color: '#0891b2',
    tag: 'SERVICE',
    intro: 'Everything systemd manages is a unit, described by a declarative .ini-style file. The most common is the .service unit (a daemon to run), but there are .socket (socket-activated services), .timer (scheduled jobs), .mount, .device, and .target units. A target is a named synchronization point that groups units — multi-user.target is the normal "system is up, multi-user, no GUI" state, the modern equivalent of an old SysV runlevel. You write a unit file, drop it in /etc/systemd/system/, run daemon-reload, and systemd manages its lifecycle.',
    deepDive: 'A .service unit has three key sections: [Unit] (description and ordering — After=, Requires=), [Service] (how to run it — ExecStart=, Restart=, User=, environment, and resource limits), and [Install] (WantedBy=, which target pulls it in when enabled). The directory precedence matters: /etc/systemd/system overrides /usr/lib/systemd/system, so packaged units are customized with drop-in overrides (systemctl edit) rather than edited in place — a clean way to change one setting without forking the whole file. Socket activation (a .socket unit) lets systemd hold the listening port and start the service on first connection, enabling lazy startup and zero-downtime restarts. Timer units are the systemd-native replacement for cron, with the advantage of journald logging and dependency awareness.',
    example: {
      scenario: 'A team needs to run a Python API as a supervised service with a restart policy and a dedicated user.',
      action: 'They write a .service unit declaring ExecStart, Restart, and User, then reload systemd and enable it.',
      code: '# /etc/systemd/system/checkout.service\n[Unit]\nDescription=Checkout API\nAfter=network-online.target\nRequires=network-online.target\n\n[Service]\nExecStart=/usr/bin/python3 /opt/checkout/app.py\nRestart=on-failure\nRestartSec=5\nUser=app\nEnvironment=PORT=8080\n\n[Install]\nWantedBy=multi-user.target\n\n$ systemctl daemon-reload && systemctl enable --now checkout',
    },
    responsibilities: [
      'Describe services, sockets, timers, and mounts declaratively',
      'Express ordering and dependencies between units',
      'Group units into targets as boot synchronization points',
      'Support drop-in overrides instead of editing packaged files',
      'Enable socket activation and timer-based scheduling',
    ],
    docUrl: 'https://www.freedesktop.org/software/systemd/man/latest/systemd.service.html',
    docLabel: 'systemd.service units',
  },
  'journald': {
    group: 'init-services',
    name: 'journald & Logging',
    short: 'The structured system journal for every service',
    color: '#16a34a',
    tag: 'LOGS',
    intro: 'systemd captures the stdout, stderr, and syslog output of every service it supervises into a single structured, indexed log: the journal, managed by systemd-journald. Instead of hunting through a dozen text files in /var/log, you query one place with journalctl, filtering by service, time, priority, or boot. Each entry carries rich metadata (the unit, PID, UID, and the original message), so you can slice logs by exactly the dimension you need. For anything running under systemd, journalctl is the first command you reach for when something misbehaves.',
    deepDive: 'The journal is binary and indexed, which is what makes filtering fast: journalctl -u nginx -p err --since "1 hour ago" is a metadata query, not a grep. -b limits to the current boot (-b -1 for the previous one — invaluable after an unexpected reboot), -f follows live like tail -f, and -k shows only kernel messages. By default the journal may be volatile (lost on reboot) unless persistent storage is enabled (/var/log/journal exists); journald.conf controls size limits and retention so logs do not fill the disk. On hosts that also run rsyslog, the journal typically forwards to it for long-term text archival or shipping to a central log store. Knowing the priority levels (emerg=0 .. debug=7) lets you cut straight to the errors.',
    example: {
      scenario: 'A service failed to start after a deploy and on-call needs its errors from this boot only.',
      action: 'They query journald scoped to the unit, error priority, and the current boot, then follow it live during a retry.',
      code: '# Errors from this unit, this boot\n$ journalctl -u checkout.service -p err -b --no-pager\nJun 05 14:02 host checkout[940]: FATAL: cannot bind :8080\nJun 05 14:02 host systemd[1]: checkout.service: Failed\n\n# Follow live while restarting\n$ journalctl -u checkout -f &\n$ systemctl restart checkout\n\n# Disk usage + vacuum old logs\n$ journalctl --disk-usage\n$ journalctl --vacuum-time=7d',
    },
    responsibilities: [
      'Capture stdout/stderr of every supervised service',
      'Store logs as structured, indexed, metadata-rich entries',
      'Filter fast by unit, priority, boot, and time',
      'Follow logs live and isolate the current or previous boot',
      'Manage retention so logs do not fill the disk',
    ],
    docUrl: 'https://www.freedesktop.org/software/systemd/man/latest/journalctl.html',
    docLabel: 'journalctl manual',
  },
  'vfs': {
    group: 'fs-permissions',
    name: 'Filesystem & VFS',
    short: 'One unified tree over many different filesystems',
    color: '#059669',
    tag: 'FILESYSTEM',
    intro: 'Linux presents everything as a single tree rooted at /, even though underneath it there are many different filesystems (ext4, XFS, Btrfs, tmpfs, network mounts) on many devices. The Virtual File System (VFS) is the kernel abstraction layer that makes this possible: it defines a common interface (open, read, write, stat) that every filesystem implements, so applications never need to know which filesystem a path lives on. The famous "everything is a file" principle flows from here — regular files, directories, devices, sockets, and /proc entries are all reached through the same path-and-file-descriptor model.',
    deepDive: 'The VFS resolves a path by walking it component by component, crossing mount points transparently — when path resolution hits a directory that has a filesystem mounted on it, it seamlessly continues into that filesystem. This is why /proc and /sys (virtual filesystems the kernel synthesizes on the fly, backed by no disk) look and behave like ordinary directories: reading /proc/meminfo is really a kernel call dressed up as a file read. The page cache sits between the VFS and the disk, caching file contents in RAM so repeated reads are memory-fast and writes can be batched. Understanding the VFS demystifies a lot: bind mounts, overlay filesystems (how container images layer), and the device files in /dev are all just the VFS exposing different backends through one consistent door.',
    example: {
      scenario: 'On-call needs to know which filesystem a path actually lives on and how full it is.',
      action: 'They use df to map the path to its mount and device, and read kernel state through the /proc virtual filesystem.',
      code: '$ df -hT /var/lib/postgresql\nFilesystem     Type  Size  Used Avail Use% Mounted on\n/dev/nvme0n1p2 ext4  200G  189G  11G   95% /var\n\n# Everything is a file: kernel state via /proc\n$ cat /proc/mounts | grep /var\n$ cat /proc/meminfo | head -3\n\n# What filesystem types are mounted?\n$ findmnt -t ext4,xfs',
    },
    responsibilities: [
      'Unify many filesystems under one root tree',
      'Expose a common file interface regardless of backend',
      'Resolve paths and cross mount points transparently',
      'Back virtual filesystems like /proc and /sys',
      'Cache file data in the page cache for speed',
    ],
    docUrl: 'https://www.kernel.org/doc/html/latest/filesystems/vfs.html',
    docLabel: 'Linux VFS overview',
  },
  'inodes': {
    group: 'fs-permissions',
    name: 'Inodes & Links',
    short: 'The metadata records behind every file name',
    color: '#d97706',
    tag: 'FILESYSTEM',
    intro: 'A file name is not the file. On disk, every file is an inode — a numbered metadata record holding the file\'s size, owner, permissions, timestamps, and the pointers to its actual data blocks. A directory is just a table mapping names to inode numbers. This separation explains a lot of Linux behavior: a hard link is a second name pointing at the same inode (same file, two names, identical until both are deleted), while a symbolic link is a tiny file whose contents are a path to another name. Deleting a file really means removing a name and decrementing the inode\'s link count; the data is freed only when the count hits zero.',
    deepDive: 'The link-count detail has a famous operational consequence: if a process still holds a file open, deleting its name does not free the space — the inode\'s link count drops but the open file descriptor keeps it alive until the process closes it or exits. This is the classic "I deleted the huge log file but df still shows the disk full" situation; lsof reveals the deleted-but-open file. A filesystem can also run out of inodes while having free space (millions of tiny files exhaust the inode table) — df -i shows inode usage, a check people forget until a "disk full" error appears on a disk with gigabytes free. Symlinks can dangle (point at a path that no longer exists); hard links cannot cross filesystem boundaries because inode numbers are per-filesystem.',
    example: {
      scenario: 'df says the disk is full but du finds nothing — a deleted file is still held open.',
      action: 'They use lsof to find deleted files still pinned by a running process, then check inode exhaustion as the other culprit.',
      code: '$ df -h /var      # 100% full\n$ du -sh /var/*   # adds up to far less than the disk\n\n# A deleted file still held open by a live process\n$ lsof +L1 | grep deleted\njava  940 app  3w  REG  253,2  9.1G  0  /var/log/app.log (deleted)\n# Reclaim by truncating the fd or restarting the process\n\n# The other cause: out of inodes, not bytes\n$ df -i /var\nInodes  IUsed   IFree  IUse%\n6.5M    6.5M     0    100%',
    },
    responsibilities: [
      'Store all file metadata and data-block pointers per inode',
      'Map names to inode numbers through directory entries',
      'Support hard links (shared inode) and symlinks (path pointer)',
      'Free data only when an inode\'s link count reaches zero',
      'Track inode exhaustion separately from byte capacity',
    ],
    docUrl: 'https://man7.org/linux/man-pages/man7/inode.7.html',
    docLabel: 'inode manual page',
  },
  'mounts': {
    group: 'fs-permissions',
    name: 'Mounts & Block Devices',
    short: 'Attaching storage devices into the filesystem tree',
    color: '#2563eb',
    tag: 'STORAGE',
    intro: 'Storage devices (disks, partitions, logical volumes, network shares) are block devices, and they do not appear in the filesystem tree until they are mounted onto a directory (a mount point). Mounting attaches a device\'s filesystem at a path: mount /dev/nvme0n1p2 /var makes that device\'s contents appear under /var. The /etc/fstab file declares which devices mount where at boot. This indirection is what lets you keep /home or /var on a separate disk, mount a network share into the tree, or swap a failing disk without changing any application paths.',
    deepDive: 'The boot-time risk lives in /etc/fstab: a wrong entry or a missing device can drop the system into emergency mode, so modern entries use stable identifiers (UUID= or LABEL=) instead of /dev/sda2, whose name can change between boots. Mount options carry real consequences — ro (read-only), noexec, nosuid, and nodev harden a mount; the wrong options can silently break an app. Logical Volume Manager (LVM) adds a layer between physical disks and filesystems so volumes can be resized and span devices. A "disk full" on one mount while another has space is the everyday reminder that the tree is assembled from independent devices. lsblk shows the whole block-device topology; mount and findmnt show what is currently attached and how.',
    example: {
      scenario: 'A new data disk must be mounted at /data and persist across reboots.',
      action: 'They inspect block devices with lsblk, mount by UUID, and add a stable fstab entry so it survives reboot.',
      code: '$ lsblk -f\nNAME        FSTYPE LABEL UUID                MOUNTPOINT\nnvme0n1\n├─nvme0n1p1 ext4         a1b2-c3d4           /\n└─nvme1n1   ext4   data  9f8e-7d6c\n\n$ mount UUID=9f8e-7d6c /data\n\n# Persist across reboots (use UUID, not /dev name)\n$ echo \'UUID=9f8e-7d6c /data ext4 defaults,nosuid 0 2\' \\\n    | sudo tee -a /etc/fstab\n$ mount -a    # validate fstab without rebooting',
    },
    responsibilities: [
      'Attach block-device filesystems onto mount points',
      'Declare persistent mounts in /etc/fstab via stable UUIDs',
      'Apply mount options like ro, noexec, nosuid for safety',
      'Expose the block-device topology (partitions, LVM)',
      'Keep application paths stable across physical storage changes',
    ],
    docUrl: 'https://man7.org/linux/man-pages/man8/mount.8.html',
    docLabel: 'mount manual page',
  },
  'permissions': {
    group: 'fs-permissions',
    name: 'Permissions & Ownership',
    short: 'Who can read, write, and execute each file',
    color: '#dc2626',
    tag: 'SECURITY',
    intro: 'Every file and directory has an owner (a user), a group, and a 9-bit permission set: read/write/execute for the owner, the group, and everyone else. When a process touches a file, the kernel checks its effective UID and GID against these bits and allows or denies the operation. This is the foundation of Linux security and the source of the single most common operational error: "Permission denied." Reading ls -l fluently — knowing that rwxr-xr-- means owner full, group read+execute, others read-only — is a daily skill.',
    deepDive: 'On directories the bits mean something subtly different: execute (x) is the right to traverse into the directory and access its entries, so a directory you can read but not execute lists names but denies access to the files inside. Beyond the basic bits, three special bits matter: setuid (run a binary as its owner — how passwd updates the shadow file as root), setgid (inherit the group, used for shared directories), and the sticky bit (on /tmp, so users can only delete their own files). chmod changes the bits (numeric 644/755 or symbolic u+x), chown changes owner and group. When the standard owner/group/other model is too coarse, POSIX ACLs (getfacl/setfacl) grant per-user permissions. A huge fraction of "it works as root but not as the service user" bugs are a directory missing the execute bit somewhere up the path.',
    example: {
      scenario: 'A service running as the app user gets "Permission denied" reading a config the file itself appears to allow.',
      action: 'They read the permission bits with ls -l, find a parent directory missing the execute bit, and fix ownership and mode.',
      code: '$ ls -l /opt/checkout/config.yaml\n-rw-r----- 1 root root 812 Jun 5 /opt/checkout/config.yaml\n# file is group-readable, but check the PATH:\n$ namei -l /opt/checkout/config.yaml\ndrwxr-x--- root root  checkout   # app user can\'t traverse!\n\n# Fix: give the service user access\n$ chown -R app:app /opt/checkout\n$ chmod 750 /opt/checkout         # owner rwx, group r-x\n$ chmod 640 /opt/checkout/config.yaml',
    },
    responsibilities: [
      'Gate read/write/execute by owner, group, and others',
      'Treat directory execute as the right to traverse',
      'Apply setuid, setgid, and the sticky bit where needed',
      'Change permissions and ownership with chmod and chown',
      'Use ACLs when owner/group/other is too coarse',
    ],
    docUrl: 'https://man7.org/linux/man-pages/man1/chmod.1.html',
    docLabel: 'chmod & permission bits',
  },
  'net-stack': {
    group: 'networking',
    name: 'Network Stack & Sockets',
    short: 'How processes send and receive data over the network',
    color: '#7c3aed',
    tag: 'NETWORK',
    intro: 'When a process talks to the network it does so through a socket — a file-descriptor handle onto the kernel\'s TCP/IP stack. A server binds a socket to a port and listens; a client connects; the kernel handles the TCP handshake, retransmission, and buffering. Because a socket is just a file descriptor, the same read()/write() model applies. Knowing what is listening on which port, and the state of each connection, is the starting point for nearly every "the service is up but unreachable" investigation.',
    deepDive: 'TCP connection state is the diagnostic gold mine. A socket in LISTEN is a server waiting; ESTABLISHED is an active connection; large numbers of TIME_WAIT are normal after closing many short connections; a pile of CLOSE_WAIT means the application is not closing sockets it should (a leak). ss (the modern replacement for netstat) shows all of this, plus the owning process with -p. The distinction between binding to 127.0.0.1 (localhost only) and 0.0.0.0 (all interfaces) is behind countless "works locally, refused remotely" bugs. The kernel also has listen and receive buffers that, when full, drop or stall connections — visible as Recv-Q/Send-Q backlog in ss. Every connection is identified by the four-tuple (source IP:port, dest IP:port), which is why one server port can hold thousands of simultaneous connections.',
    example: {
      scenario: 'A service is running but clients get "connection refused" — on-call must confirm what it is actually listening on.',
      action: 'They use ss to list listening sockets with owning processes, exposing a bind to localhost instead of all interfaces.',
      code: '$ ss -tlnp\nState   Local Address:Port   Process\nLISTEN  127.0.0.1:8080       users:(("checkout",pid=940))\n# Bound to localhost only -> refused from other hosts\n\n# Connections to this port, by state\n$ ss -tan \'( dport = :8080 or sport = :8080 )\' | \\\n    awk \'{print $1}\' | sort | uniq -c\n# A leak signature:\n# 4012 CLOSE_WAIT   <-- app not closing sockets',
    },
    responsibilities: [
      'Expose the TCP/IP stack to processes via sockets',
      'Manage handshakes, retransmission, and buffering',
      'Track connection state (LISTEN, ESTABLISHED, TIME_WAIT)',
      'Distinguish localhost binds from all-interface binds',
      'Identify each connection by its source/dest four-tuple',
    ],
    docUrl: 'https://man7.org/linux/man-pages/man8/ss.8.html',
    docLabel: 'ss socket statistics',
  },
  'dns-routing': {
    group: 'networking',
    name: 'Routing & DNS Resolution',
    short: 'How packets find their way and names become addresses',
    color: '#9333ea',
    tag: 'NETWORK',
    intro: 'Two questions sit behind every outbound connection: what IP address does this name resolve to, and which interface and gateway do I send the packet through? Name resolution turns checkout.internal into an IP via /etc/hosts and DNS servers (configured through /etc/resolv.conf or systemd-resolved). The routing table then decides where each packet goes: directly to a host on the local subnet, or via the default gateway to the wider network. When connectivity is broken, isolating "is it DNS?" from "is it routing?" is the first and most valuable fork in the diagnosis.',
    deepDive: 'The resolution path is more layered than it looks: nsswitch.conf decides the order of sources (files, then DNS), so /etc/hosts can override DNS, and a stale entry there causes baffling "wrong IP" bugs. systemd-resolved adds local caching, which means a DNS change can appear to "not take" until the cache is flushed. On the routing side, ip route shows the table; the default route (0.0.0.0/0) is the catch-all gateway, and a missing or wrong default route means you can reach the local subnet but nothing beyond it. The old net-tools (ifconfig, route, netstat) are deprecated in favor of the ip suite (ip addr, ip route, ip neigh). "It\'s always DNS" is a running joke precisely because resolution failures masquerade as application errors — dig and getent hosts separate the two cleanly.',
    example: {
      scenario: 'A host cannot reach an internal API — on-call must determine whether it is DNS or routing.',
      action: 'They resolve the name with dig and getent, then inspect the routing table and default gateway with the ip suite.',
      code: '# Is it DNS? Resolve the name explicitly\n$ getent hosts checkout.internal      # uses nsswitch order\n$ dig +short checkout.internal        # asks DNS directly\n;; no answer  -> DNS problem\n\n# Is it routing? Check the path to the IP\n$ ip route get 10.20.0.5\n10.20.0.5 via 10.0.0.1 dev eth0\n$ ip route show default               # default gateway present?\n$ ip addr show eth0                    # interface up + has IP?',
    },
    responsibilities: [
      'Resolve names to addresses via hosts file and DNS',
      'Honor nsswitch ordering and resolver caching',
      'Decide each packet\'s interface and next hop via the route table',
      'Provide a default gateway for non-local destinations',
      'Separate name-resolution failures from routing failures',
    ],
    docUrl: 'https://man7.org/linux/man-pages/man8/ip-route.8.html',
    docLabel: 'ip route manual',
  },
  'firewall': {
    group: 'networking',
    name: 'Firewall & netfilter',
    short: 'Kernel packet filtering that allows or blocks traffic',
    color: '#0891b2',
    tag: 'NETWORK',
    intro: 'The Linux kernel filters, mangles, and translates network packets through netfilter — the framework behind iptables and its successor nftables. Rules decide which packets are accepted, dropped, or rejected, and which are rewritten (NAT). This is what enforces "only port 443 is open to the world," what does the address translation behind a home router, and what container runtimes program to wire pod networking. When traffic that should arrive does not — and the routing and the listening socket both check out — a DROP rule is the usual culprit.',
    deepDive: 'netfilter organizes rules into tables (filter for allow/deny, nat for translation) and chains hooked at points in the packet path (INPUT for packets destined to the host, FORWARD for routed-through traffic, OUTPUT for locally generated). The critical and confusing distinction is DROP versus REJECT: DROP silently discards the packet, so the client hangs until timeout (a classic "it just hangs" symptom), while REJECT sends an explicit refusal and fails fast. Rule order matters — chains are evaluated top to bottom and the first match wins, so a broad DROP above a specific ACCEPT shadows it. Most distributions now ship a higher-level front end (firewalld or ufw) that generates nftables rules, but underneath it is all netfilter, and iptables -L -n -v / nft list ruleset reveal the truth including per-rule packet counters that show what is actually being hit.',
    example: {
      scenario: 'Remote clients time out connecting to a service that is confirmed listening on all interfaces.',
      action: 'They list the firewall rules with packet counters, find a DROP shadowing the service port, and add an explicit ACCEPT.',
      code: '$ sudo iptables -L INPUT -n -v --line-numbers\nnum  pkts target  prot  dpt\n1    9821 ACCEPT  tcp   22\n2       0 ACCEPT  tcp   8080      # 0 packets -> never reached\n3    4012 DROP    all             # broad DROP above it shadows 8080\n\n# Fix: insert an ACCEPT before the catch-all DROP\n$ sudo iptables -I INPUT 2 -p tcp --dport 8080 -j ACCEPT\n# DROP = client hangs; REJECT = client fails fast',
    },
    responsibilities: [
      'Filter packets at INPUT, FORWARD, and OUTPUT hooks',
      'Accept, drop, or reject traffic by rule',
      'Perform NAT and port translation',
      'Evaluate chains in order with first-match-wins',
      'Distinguish silent DROP from explicit REJECT behavior',
    ],
    docUrl: 'https://wiki.nftables.org/wiki-nftables/index.php/Main_Page',
    docLabel: 'nftables wiki',
  },
  'proc-tools': {
    group: 'troubleshooting',
    name: 'Process & CPU Tools',
    short: 'Finding what is eating CPU, memory, and syscalls',
    color: '#dc2626',
    tag: 'TROUBLESHOOT',
    intro: 'When a box is slow or a process misbehaves, a handful of tools answer "what is it doing right now?" top and its friendlier cousin htop give a live view of CPU and memory per process; ps takes a one-shot snapshot you can script and sort; strace shows the exact system calls a process is making (and where they fail); and /proc exposes the kernel\'s per-process truth. Fluency here means turning a vague "the server is slow" into a specific "PID 940 is spinning on a failing open() of a missing file" in under a minute.',
    deepDive: 'Each tool has a sweet spot. top -o %CPU or htop sorts live to find the hog; press to toggle per-core view and thread view. ps -eo pid,ppid,%cpu,%mem,rss,stat,comm --sort=-%cpu is the scriptable snapshot for the top consumers. strace -f -p PID attaches to a running process and prints every syscall — invaluable for "why is this hanging?" (you will see it blocked in read() or stuck retrying connect()); strace -c summarizes which syscalls dominate and which are erroring. For deeper performance work, perf top profiles where CPU cycles actually go. The /proc filesystem underlies all of them: /proc/PID/status, /proc/PID/fd, /proc/PID/limits, and /proc/PID/stack are the raw kernel records these tools format. The discipline is to go from broad (top) to specific (strace on one PID) rather than guessing.',
    example: {
      scenario: 'One process is pinning a CPU core and on-call needs to know what syscall it is stuck on.',
      action: 'They find the hog with ps sorted by CPU, then attach strace to see it spinning on a failing system call.',
      code: '$ ps -eo pid,%cpu,%mem,stat,comm --sort=-%cpu | head -4\n  PID %CPU %MEM STAT COMMAND\n 2231 99.3  1.2 R    report\n\n# What is it actually doing?\n$ sudo strace -f -p 2231 2>&1 | head\nopenat(AT_FDCWD, "/etc/missing.conf", O_RDONLY) = -1 ENOENT\nopenat(AT_FDCWD, "/etc/missing.conf", O_RDONLY) = -1 ENOENT\n# tight retry loop on a missing file\n\n# Summarize syscalls + errors\n$ sudo strace -c -p 2231',
    },
    responsibilities: [
      'Give a live per-process view of CPU and memory (top/htop)',
      'Snapshot and sort consumers scriptably (ps)',
      'Trace the exact syscalls a process makes (strace)',
      'Surface failing calls and hang points',
      'Expose raw kernel state through /proc/PID',
    ],
    docUrl: 'https://man7.org/linux/man-pages/man1/strace.1.html',
    docLabel: 'strace manual page',
  },
  'io-tools': {
    group: 'troubleshooting',
    name: 'Disk & I/O Tools',
    short: 'Diagnosing full disks, slow I/O, and open files',
    color: '#b91c1c',
    tag: 'TROUBLESHOOT',
    intro: 'Storage problems show up constantly: a disk fills, writes slow to a crawl, or a "deleted" file refuses to free space. The core toolkit is df (per-filesystem capacity, both bytes and inodes), du (where the space is actually going within a tree), lsof (which process has which file open), and iostat / iotop (live disk throughput and which process is driving it). Most "disk full" pages are solved by these four in sequence — and reading them correctly avoids the trap of deleting the wrong thing.',
    deepDive: 'The order matters. Start with df -h to find the full mount and df -i to rule out inode exhaustion (full disk, gigabytes free, but no inodes left — common with mail spools and tiny-file caches). Then du -xh --max-depth=1 /mount | sort -h walks down to the biggest directories without crossing into other filesystems (the -x flag prevents du from wandering off the mount and giving misleading totals). If df and du disagree — df full, du small — a process is holding a deleted file open, and lsof +L1 finds it (the space frees on process restart or by truncating the descriptor). For slow rather than full disks, iostat -xz 1 shows per-device utilization and await (average I/O latency); a device pinned at %util near 100 with high await is your bottleneck, and iotop -o reveals which process is responsible.',
    example: {
      scenario: 'A "disk full" alert fired and on-call must find the space before the service crashes.',
      action: 'They locate the full mount with df, drill into the biggest directory with du, and check for a deleted-but-open file when the numbers disagree.',
      code: '$ df -h && df -i        # bytes AND inodes\n/dev/nvme0n1p2  200G  198G  2G  99%  /var\n\n# Biggest dirs under the full mount (stay on one fs)\n$ du -xh --max-depth=1 /var | sort -h | tail -5\n178G  /var/log\n\n# If df disagrees with du -> deleted file held open\n$ sudo lsof +L1 /var | grep deleted\n# Slow, not full? Find the I/O hog:\n$ iostat -xz 1 3        # watch %util and await\n$ sudo iotop -o',
    },
    responsibilities: [
      'Report per-filesystem byte and inode capacity (df)',
      'Locate where space is consumed within a tree (du -x)',
      'Find which process holds which file open (lsof)',
      'Reconcile df-vs-du gaps from deleted-open files',
      'Measure disk latency and the driving process (iostat/iotop)',
    ],
    docUrl: 'https://man7.org/linux/man-pages/man8/lsof.8.html',
    docLabel: 'lsof manual page',
  },
  'net-tools': {
    group: 'troubleshooting',
    name: 'Network Tools',
    short: 'Tracing connectivity from socket to packet',
    color: '#ea580c',
    tag: 'TROUBLESHOOT',
    intro: 'Network debugging is a top-to-bottom drill: is the name resolving, is there a route, is the port open, is something listening, and are packets actually flowing? The toolkit maps onto those layers — dig/getent for DNS, ip for addresses and routes, ping for reachability, ss for sockets and listeners, curl for the application request, and tcpdump for the raw packets when all else looks fine but it still does not work. Reaching for them in order turns "the service is unreachable" from a guessing game into a deterministic checklist.',
    deepDive: 'A reliable sequence saves hours. (1) Resolve: getent hosts host or dig +short — wrong or empty answer means DNS, stop here. (2) Reach: ping the IP (note ICMP may be firewalled, so a failed ping is not conclusive). (3) Port: ss -tlnp on the server confirms something is listening on the expected address; nc -zv host port or curl -v from the client confirms the port is reachable. (4) Application: curl -v exposes TLS handshakes, redirects, and HTTP status separate from connectivity. (5) Packets: when everything above looks right but it still fails, tcpdump -ni eth0 port 8080 shows whether SYNs arrive and whether they are answered — the ground truth that distinguishes "packets never arrive" (routing/firewall) from "arrive but reset" (application). curl --resolve and the -4/-6 flags isolate IPv4-vs-IPv6 surprises, another frequent culprit.',
    example: {
      scenario: 'A client cannot reach an API and on-call needs to localize the failure layer by layer.',
      action: 'They walk the stack: resolve the name, test the port, run the HTTP request verbosely, then sniff packets to see if SYNs are answered.',
      code: '# 1. DNS\n$ dig +short api.internal        ->  10.20.0.5\n# 2. Port reachable from the client?\n$ nc -zv 10.20.0.5 8080\nConnection to 10.20.0.5 8080 ... succeeded!\n# 3. Application layer\n$ curl -v https://api.internal/health\n< HTTP/1.1 200 OK\n# 4. If it hangs, watch the packets on the server\n$ sudo tcpdump -ni eth0 port 8080\n14:02 IP client.51000 > 10.20.0.5.8080: Flags [S]   # SYN in\n14:02 IP 10.20.0.5.8080 > client.51000: Flags [R]   # RST = app refused',
    },
    responsibilities: [
      'Resolve names and confirm the answer (dig/getent)',
      'Verify interfaces, addresses, and routes (ip/ping)',
      'Confirm a listener and port reachability (ss/nc/curl)',
      'Separate application errors from connectivity (curl -v)',
      'Inspect raw packet flow as ground truth (tcpdump)',
    ],
    docUrl: 'https://man7.org/linux/man-pages/man1/tcpdump.1.html',
    docLabel: 'tcpdump manual page',
  },
};

export const LINUX_GROUPS = [
  {
    id: 'processes-memory',
    title: 'Processes & Memory',
    color: '#2563eb',
    colorDark: '#1e3a8a',
    gradient: 'linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%)',
    bg: '#eff6ff',
    components: ['process', 'scheduler', 'memory'],
  },
  {
    id: 'init-services',
    title: 'Init & Services',
    color: '#0d9488',
    colorDark: '#115e59',
    gradient: 'linear-gradient(135deg, #ccfbf1 0%, #99f6e4 100%)',
    bg: '#f0fdfa',
    components: ['systemd', 'units', 'journald'],
  },
  {
    id: 'fs-permissions',
    title: 'Filesystem & Permissions',
    color: '#059669',
    colorDark: '#064e3b',
    gradient: 'linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%)',
    bg: '#f0fdf4',
    components: ['vfs', 'inodes', 'mounts', 'permissions'],
  },
];

export const LINUX_GROUP_COLOR_MAP = {
  'process':     '#2563eb',
  'scheduler':   '#2563eb',
  'memory':      '#2563eb',
  'systemd':     '#0d9488',
  'units':       '#0d9488',
  'journald':    '#0d9488',
  'vfs':         '#059669',
  'inodes':      '#059669',
  'mounts':      '#059669',
  'permissions': '#059669',
};

export const LINUX_SECONDARY_SECTIONS = [
  { id: 'networking',      title: 'Networking',             color: '#7c3aed', components: ['net-stack', 'dns-routing', 'firewall'] },
  { id: 'troubleshooting', title: 'Troubleshooting Toolkit', color: '#dc2626', components: ['proc-tools', 'io-tools', 'net-tools'] },
];

export const LINUX_FLOWS = [
  ['systemd',   'units'],
  ['systemd',   'journald'],
  ['units',     'process'],
  ['process',   'scheduler'],
  ['process',   'memory'],
  ['process',   'vfs'],
  ['vfs',       'inodes'],
  ['vfs',       'mounts'],
  ['vfs',       'permissions'],
];

export const LINUX_JOURNEY = [
  { title: 'Kernel hands off to systemd', desc: 'The kernel mounts the root filesystem and starts PID 1 — systemd takes over user space.', actor: 'kernel' },
  { title: 'systemd reaches the target',  desc: 'Units are activated in dependency order to bring the system to multi-user.target.', actor: 'systemd' },
  { title: 'A service unit starts',       desc: 'systemd reads the .service unit and launches the daemon as a supervised process.', actor: 'unit' },
  { title: 'fork() + exec()',             desc: 'The process is created by cloning the parent and replacing it with the program image.', actor: 'process' },
  { title: 'Scheduler runs it',           desc: 'CFS time-slices the process onto a CPU according to its nice value and priority.', actor: 'scheduler' },
  { title: 'Virtual memory backs it',     desc: 'Pages are mapped on demand and the MMU translates virtual addresses to RAM.', actor: 'memory' },
  { title: 'File I/O through the VFS',     desc: 'open() walks the path, permission bits are checked, and a file descriptor is returned.', actor: 'vfs' },
  { title: 'Logs to journald',            desc: 'The process’s stdout and stderr are captured as structured entries in the journal.', actor: 'journald' },
  { title: 'Exit & reaping',              desc: 'The process exits; its parent reaps the status and the PID slot is freed.', actor: 'exit' },
];

export const LINUX_TOPIC = {
    id: 'linux',
    name: 'Linux Internals',
    short: 'Processes, systemd, filesystems, networking, and permissions.',
    accent: '#334155',
    accentSoft: '#e2e8f0',
    eyebrow: 'CloudTruck · Linux internals · from boot to running process',
    heroTitle: 'The Linux system, from PID 1 to the prompt.',
    heroLead: 'Trace how a Linux box actually works: processes and the scheduler, virtual memory, systemd and its units, the filesystem and permissions, the network stack, and the troubleshooting commands that tie it all together. Click any component to learn what it does — from first principles to the exact command you would run at 3am.',
    heroStats: ['16 components', 'processes · systemd · fs · net', 'troubleshooting toolkit'],
    diagramLabel: 'HOST · prod-01 · boot-to-process graph',
    panelHint: 'trace the process flow ->',
    sectionsTitle: 'Networking and the troubleshooting toolkit',
    sectionsLead: 'Beyond processes and files, a real Linux box lives on the network and breaks in production. The socket stack, routing and DNS, the firewall, and the day-to-day troubleshooting commands (top, strace, lsof, ss, tcpdump) are how you actually keep it running.',
    workflowTitle: 'The journey of a process',
    workflowLead: 'Follow the system from kernel hand-off through systemd, fork/exec, scheduling, memory, file I/O, logging, and exit.',
    workflowCommand: 'systemctl start checkout.service',
    workflowRealtimeLabel: 'Live process trace',
    compareTitle: 'The mental model',
    compareParagraphs: [
      'Linux is a small set of ideas repeated everywhere: everything is a process or a file, the kernel mediates every resource through system calls, and isolation comes from per-process address spaces and credentials. Once that clicks, "the server is slow" stops being mysterious and becomes a question of which subsystem — CPU, memory, disk, or network — is under pressure.',
      'systemd boots and supervises, the VFS unifies storage behind one tree, sockets expose the network as file descriptors, and permission bits gate every access. The troubleshooting tools are just windows onto these same subsystems — top reads the scheduler, lsof reads the file table, ss reads the socket table — so learning the internals and learning to debug are the same skill.',
    ],
    footerTicker: 'LINUX · KERNEL · SYSTEMD · VFS · SOCKETS · PERMISSIONS · /PROC',
    emptySearch: 'No Linux component matched that search.',
    comingSoonCopy: 'This future topic will get the same interactive treatment.',
    components: LINUX_COMPONENTS,
    groups: LINUX_GROUPS,
    groupColorMap: LINUX_GROUP_COLOR_MAP,
    secondarySections: LINUX_SECONDARY_SECTIONS,
    flows: LINUX_FLOWS,
    journey: LINUX_JOURNEY,
};

export default LINUX_TOPIC;
