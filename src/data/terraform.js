/* Terraform Topic Data — CloudTruck */

export const TERRAFORM_COMPONENTS = {
  'hcl-config': {
    group: 'authoring',
    name: 'HCL Configuration',
    short: 'Declarative .tf files describing the desired infrastructure',
    color: '#7b42bc',
    tag: 'CONFIG',
    intro: 'Everything in Terraform starts with HCL (HashiCorp Configuration Language) — human-readable .tf files where you declare the infrastructure you want to exist. You do not write steps ("create a server, then attach a disk"); you declare end state ("a server with this disk attached") and Terraform figures out the API calls. A resource block maps to one real object (an AWS EC2 instance, a GCS bucket, a Cloudflare DNS record). Variables parameterize it, outputs expose values, and data sources read things Terraform does not manage. This declarative model is the entire reason Terraform is reproducible.',
    deepDive: 'HCL is more than key-value config: it has expressions, functions, conditionals, for-expressions, and dynamic blocks. The dependency graph is built implicitly from references — when resource B uses resource A\'s id (aws_subnet.main.id), Terraform knows A must exist first; you almost never need explicit depends_on. Keep configs DRY with locals and modules, but resist over-abstraction: a module so generic that every resource is a variable is harder to read than plain HCL. count and for_each turn one block into many, but for_each (keyed by a stable map) is strongly preferred over count (keyed by list index) because removing the middle element of a count list re-indexes and destroys everything after it.',
    example: {
      scenario: 'A team needs an S3 bucket for application logs, with versioning enabled and a tag set.',
      action: 'They declare a single aws_s3_bucket resource plus a versioning sub-resource. Terraform reads this and computes the API calls needed to make it real.',
      code: 'resource "aws_s3_bucket" "logs" {\n  bucket = "acme-app-logs-${var.environment}"\n\n  tags = {\n    Environment = var.environment\n    ManagedBy   = "terraform"\n  }\n}\n\nresource "aws_s3_bucket_versioning" "logs" {\n  bucket = aws_s3_bucket.logs.id\n  versioning_configuration {\n    status = "Enabled"\n  }\n}\n\noutput "log_bucket_name" {\n  value = aws_s3_bucket.logs.bucket\n}',
    },
    responsibilities: [
      'Declare the desired end state of every managed resource',
      'Parameterize configurations with variables, locals, and outputs',
      'Read unmanaged infrastructure through data sources',
      'Establish implicit dependencies through attribute references',
      'Keep configuration in version control alongside application code',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/language',
    docLabel: 'Terraform language documentation',
  },
  'providers': {
    group: 'authoring',
    name: 'Providers',
    short: 'Plugins that translate HCL into real cloud API calls',
    color: '#0891b2',
    tag: 'PLUGIN',
    intro: 'A provider is the plugin that knows how to talk to a specific platform — AWS, Google Cloud, Azure, Kubernetes, GitHub, Datadog. Each provider exposes resource types (aws_instance, google_storage_bucket) and data sources, and handles authentication, the actual API requests, and mapping API responses back into Terraform state. Terraform itself is just a graph engine and a state machine; providers are where all the platform-specific logic lives. There are thousands of them in the Terraform Registry.',
    deepDive: 'Provider version pinning is the single most important habit for stable infrastructure. Always set required_providers with a version constraint (~> 5.0 allows 5.x but not 6.0) and commit the .terraform.lock.hcl lock file — it records exact versions and checksums so every teammate and CI run uses identical provider binaries. Provider configuration (region, credentials, assume-role) is separate from version. Use provider aliases when you need the same provider configured differently — e.g. deploying to us-east-1 and eu-west-1 in one config, or managing two AWS accounts. Never hardcode credentials in provider blocks; rely on environment variables, instance profiles, or OIDC.',
    example: {
      scenario: 'A configuration must deploy resources to two AWS regions from a single root module.',
      action: 'The team pins the AWS provider version, then declares a default provider for us-east-1 and an aliased provider for eu-west-1. Resources select a provider explicitly when needed.',
      code: 'terraform {\n  required_providers {\n    aws = {\n      source  = "hashicorp/aws"\n      version = "~> 5.0"\n    }\n  }\n}\n\nprovider "aws" {\n  region = "us-east-1"\n}\n\nprovider "aws" {\n  alias  = "europe"\n  region = "eu-west-1"\n}\n\nresource "aws_s3_bucket" "eu_logs" {\n  provider = aws.europe\n  bucket   = "acme-eu-logs"\n}',
    },
    responsibilities: [
      'Authenticate to the target platform and make all API calls',
      'Expose resource types and data sources as HCL blocks',
      'Map provider API responses into the Terraform state model',
      'Honor version constraints recorded in the dependency lock file',
      'Support aliases for multi-region and multi-account deployments',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/language/providers',
    docLabel: 'Providers documentation',
  },
  'modules': {
    group: 'authoring',
    name: 'Modules',
    short: 'Reusable, versioned packages of resources called like functions',
    color: '#d97706',
    tag: 'REUSE',
    intro: 'A module is a folder of .tf files you can call from other configurations, passing inputs and reading outputs — like a function for infrastructure. Every Terraform config is already a module (the root module). The power comes from reuse: define a "vpc" module once with sensible defaults, then instantiate it for dev, staging, and prod with three different variable sets. Modules can come from the local filesystem, a Git repository, or the Terraform Registry, and they should be versioned so an upgrade is an explicit, reviewable change.',
    deepDive: 'Good module design is an interface design problem: expose the inputs that genuinely vary and hide the rest behind sane defaults. A module with 60 variables is a leaky abstraction. Pin registry and Git module sources to a version or tag (ref=v1.4.0) — pulling a module from a moving branch means your infrastructure can change without any change to your own code. Beware deeply nested module trees: they make the plan output hard to read and resource addresses unwieldy (module.platform.module.network.aws_subnet.this[0]). Most teams find two levels of nesting is the practical limit. Composition (root module wiring several flat modules together) usually beats deep inheritance.',
    example: {
      scenario: 'Three environments each need an identical VPC layout, differing only in CIDR range and environment name.',
      action: 'The team writes one network module and instantiates it three times with different inputs, pinning the module version for safety.',
      code: 'module "vpc" {\n  source  = "terraform-aws-modules/vpc/aws"\n  version = "~> 5.0"\n\n  name = "acme-${var.environment}"\n  cidr = var.vpc_cidr\n\n  azs             = ["us-east-1a", "us-east-1b"]\n  private_subnets = var.private_subnets\n  public_subnets  = var.public_subnets\n\n  enable_nat_gateway = true\n}\n\noutput "vpc_id" {\n  value = module.vpc.vpc_id\n}',
    },
    responsibilities: [
      'Package related resources behind a clean input/output interface',
      'Enable reuse of the same infrastructure pattern across environments',
      'Pin to explicit versions so upgrades are reviewable changes',
      'Hide incidental complexity behind sensible default values',
      'Compose larger systems from small, focused building blocks',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/language/modules',
    docLabel: 'Modules documentation',
  },
  'terraform-init': {
    group: 'core-workflow',
    name: 'terraform init',
    short: 'Bootstraps a working directory: backend, providers, modules',
    color: '#2563eb',
    tag: 'COMMAND',
    intro: 'terraform init is the first command you run in any new or freshly cloned configuration. It does three things: initializes the backend (where state is stored), downloads the provider plugins declared in required_providers, and fetches every module source. It writes the .terraform/ directory and the .terraform.lock.hcl lock file. Until init has run, no other command works — plan and apply both depend on the providers and backend it sets up. It is safe to re-run: init is idempotent and you run it again whenever you add a provider, add a module, or change the backend.',
    deepDive: 'init is where the dependency lock file (.terraform.lock.hcl) is created and updated. Commit this file — it pins exact provider versions and checksums, guaranteeing reproducible runs across machines and CI. Use terraform init -upgrade deliberately when you want to move to newer provider versions within your constraints; without it, init respects the locked versions. In CI, cache the .terraform/providers directory to avoid re-downloading hundreds of megabytes every run. terraform init -backend-config=... injects backend settings (bucket, key, region) at init time so the same code can target different state locations per environment. A migration prompt appears if you change backends — init offers to copy existing state to the new location.',
    example: {
      scenario: 'A new engineer clones the infrastructure repo and needs to start making changes.',
      action: 'They run terraform init, which configures the S3 backend, downloads the AWS provider, fetches the VPC module, and writes the lock file.',
      code: '$ terraform init\n\nInitializing the backend...\nInitializing modules...\n- vpc in .terraform/modules/vpc\n\nInitializing provider plugins...\n- Finding hashicorp/aws versions matching "~> 5.0"...\n- Installing hashicorp/aws v5.42.0...\n- Installed hashicorp/aws v5.42.0\n\nTerraform has been successfully initialized!',
    },
    responsibilities: [
      'Configure and connect to the state backend',
      'Download provider plugins matching the version constraints',
      'Fetch all module sources into the working directory',
      'Create and update the .terraform.lock.hcl dependency lock file',
      'Prepare the directory so plan and apply can run',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/cli/commands/init',
    docLabel: 'terraform init command',
  },
  'terraform-plan': {
    group: 'core-workflow',
    name: 'terraform plan',
    short: 'Computes the diff between desired config and real-world state',
    color: '#16a34a',
    tag: 'COMMAND',
    intro: 'terraform plan is the dry run — and the most important safety feature in the entire tool. It reads your HCL, reads the current state, refreshes against the real platform, then computes exactly what it would change: what to add (+), what to change (~), and what to destroy (-). Nothing is modified. The plan is what you review before touching production. A green "0 to add, 0 to change, 0 to destroy" means your config matches reality. Anything else is a change you must understand before applying.',
    deepDive: 'Always save the plan to a file (terraform plan -out=tfplan) and apply that exact file — this guarantees the apply does precisely what you reviewed, with no window for drift between plan and apply. The most dangerous lines in any plan are the ones marked for destruction or "forces replacement": changing an immutable attribute (like an EC2 instance\'s AMI or a database\'s name) destroys and recreates the resource, which can mean data loss or downtime. Read those carefully. In CI, post the plan output as a PR comment so reviewers approve infrastructure changes the same way they approve code. terraform plan -detailed-exitcode returns 2 when there are changes, letting pipelines branch on "drift detected."',
    example: {
      scenario: 'An engineer bumps an instance type from t3.medium to t3.large and wants to confirm the blast radius before applying.',
      action: 'They run plan with -out. The output shows an in-place update (~), confirming no destroy/recreate — safe to apply.',
      code: '$ terraform plan -out=tfplan\n\nTerraform will perform the following actions:\n\n  # aws_instance.api will be updated in-place\n  ~ resource "aws_instance" "api" {\n      ~ instance_type = "t3.medium" -> "t3.large"\n        id            = "i-0abc123def456"\n        # (28 unchanged attributes hidden)\n    }\n\nPlan: 0 to add, 1 to change, 0 to destroy.\n\nSaved the plan to: tfplan',
    },
    responsibilities: [
      'Refresh state against the real platform and compute a diff',
      'Show exactly what will be added, changed, or destroyed',
      'Flag replacements that destroy and recreate resources',
      'Produce a saved plan file for a guaranteed-faithful apply',
      'Drive human review and CI policy checks before any change',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/cli/commands/plan',
    docLabel: 'terraform plan command',
  },
  'terraform-apply': {
    group: 'core-workflow',
    name: 'terraform apply',
    short: 'Executes the plan, making real changes to infrastructure',
    color: '#dc2626',
    tag: 'COMMAND',
    intro: 'terraform apply is where intent becomes reality. It takes a plan (either freshly computed or a saved -out file), acquires a state lock, and walks the dependency graph making the actual provider API calls — creating, updating, and destroying resources in the correct order and in parallel where it safely can. As each resource succeeds, its real-world attributes (the generated id, IP address, ARN) are written back to state. When apply finishes, the world matches your configuration and state records exactly what exists.',
    deepDive: 'apply executes the graph with bounded parallelism (default 10, tunable with -parallelism). It locks state for the entire run so two applies cannot race. If a resource fails partway through, Terraform stops, records everything created so far in state, and leaves you in a partially-applied state — the next plan shows what remains. This is why state integrity matters so much: a lost or corrupted state after a partial apply means Terraform no longer knows what it created. In automation, apply a saved plan file and never use -auto-approve on interactive production runs. Treat destroy-and-recreate operations as deployments, not config tweaks: they may need a maintenance window.',
    example: {
      scenario: 'The reviewed plan for the instance resize is approved and ready to execute in CI.',
      action: 'The pipeline applies the exact saved plan file. Terraform locks state, performs the in-place update, and records the new attributes.',
      code: '$ terraform apply tfplan\n\naws_instance.api: Modifying... [id=i-0abc123def456]\naws_instance.api: Still modifying... [10s elapsed]\naws_instance.api: Modifications complete after 14s\n\nApply complete! Resources: 0 added, 1 changed, 0 destroyed.\n\nOutputs:\n\napi_public_ip = "54.210.11.42"',
    },
    responsibilities: [
      'Acquire a state lock for the duration of the run',
      'Walk the dependency graph and call provider APIs in order',
      'Create, update, and destroy resources to match the plan',
      'Write real-world resource attributes back into state',
      'Surface outputs and report the final add/change/destroy counts',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/cli/commands/apply',
    docLabel: 'terraform apply command',
  },
  'state-file': {
    group: 'state-backend',
    name: 'State File',
    short: 'The source of truth mapping config to real resources',
    color: '#059669',
    tag: 'STATE',
    intro: 'The state file (terraform.tfstate) is Terraform\'s memory. It is a JSON document that maps every resource in your config to the real object it created, recording the resource\'s ID and all its known attributes. Without state, Terraform would have no way to know that aws_instance.api in your code is the EC2 instance i-0abc123 in the cloud — it would try to create a second one. Every plan diffs config against state; every apply updates state. State is the most important and most fragile artifact in the whole system.',
    deepDive: 'State frequently contains secrets in plaintext — database passwords, private keys, generated tokens — because providers store every attribute. This is why state must never live in a public bucket and should be encrypted at rest. Never hand-edit the JSON; use the state subcommands (terraform state list, show, mv, rm) instead. terraform state mv is essential when you refactor code (renaming a resource or moving it into a module) without destroying and recreating the real infrastructure. terraform state rm removes Terraform\'s tracking of a resource without deleting it in the cloud. Sensitive outputs and state make local state on a laptop a liability — which is exactly why teams move to a remote backend.',
    example: {
      scenario: 'An engineer renames a resource from "web" to "api" in the code and must avoid destroying the live server.',
      action: 'Instead of letting Terraform destroy+recreate, they use terraform state mv to update the address in state, so the next plan shows no changes.',
      code: '# Renamed in code: aws_instance.web -> aws_instance.api\n\n$ terraform state mv \\\n    aws_instance.web aws_instance.api\nMove "aws_instance.web" to "aws_instance.api"\nSuccessfully moved 1 object(s).\n\n$ terraform plan\nNo changes. Your infrastructure matches the configuration.',
    },
    responsibilities: [
      'Map each configured resource to its real-world object and ID',
      'Cache resource attributes so plans can compute diffs',
      'Track resource dependencies and metadata',
      'Serve as the single source of truth for what Terraform manages',
      'Be edited only through terraform state subcommands, never by hand',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/language/state',
    docLabel: 'Terraform state documentation',
  },
  'remote-backend': {
    group: 'state-backend',
    name: 'Remote Backend',
    short: 'Shared, encrypted, durable storage for team state',
    color: '#0d9488',
    tag: 'BACKEND',
    intro: 'A backend determines where Terraform stores state and runs operations. The default is local — a terraform.tfstate file on your disk — which is fine for solo experiments and a disaster for teams (whose state? what if two people apply at once? what if the laptop dies?). A remote backend (S3, Google Cloud Storage, Azure Blob, Terraform Cloud) stores state in shared, durable, encrypted storage that everyone reads from and writes to. This is the prerequisite for any collaboration: one canonical state, accessible to every engineer and every CI run.',
    deepDive: 'The classic AWS pattern is an S3 backend for storage plus DynamoDB for locking (newer AWS provider versions support native S3 lockfile locking, removing the DynamoDB requirement). Enable bucket versioning so you can recover from a corrupted or accidentally-deleted state, and enable server-side encryption because state holds secrets. Use a backend key that encodes environment and component (env/prod/network/terraform.tfstate) so each stack has its own isolated state — never one giant monolithic state for an entire organization, which makes every plan slow and every apply high-risk. Backend configuration cannot use variables (it is read before variables are evaluated), so per-environment backends are supplied with -backend-config files at init time.',
    example: {
      scenario: 'A team moves from local state on laptops to a shared S3 backend with locking.',
      action: 'They declare an S3 backend with versioning and encryption enabled, then run init to migrate the existing local state into the bucket.',
      code: 'terraform {\n  backend "s3" {\n    bucket       = "acme-tfstate-prod"\n    key          = "network/terraform.tfstate"\n    region       = "us-east-1"\n    encrypt      = true\n    use_lockfile = true\n  }\n}\n\n# Migrate existing local state:\n$ terraform init -migrate-state',
    },
    responsibilities: [
      'Store state in durable, shared, encrypted remote storage',
      'Give every engineer and CI run access to one canonical state',
      'Enable versioning so corrupted state can be recovered',
      'Isolate state per environment and component via the backend key',
      'Provide the foundation for locking and team collaboration',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/language/backend',
    docLabel: 'Backend configuration',
  },
  'state-locking': {
    group: 'state-backend',
    name: 'State Locking',
    short: 'Prevents concurrent applies from corrupting state',
    color: '#ea580c',
    tag: 'SAFETY',
    intro: 'State locking stops two people (or two pipelines) from running apply against the same state at the same time. Without it, concurrent writes can interleave and corrupt the state file, leaving Terraform with an inconsistent picture of reality — the single most painful failure mode in Terraform operations. When you run apply, Terraform acquires a lock; anyone else who tries gets an error telling them the state is locked, by whom, and since when. The lock is released when the operation finishes.',
    deepDive: 'Most backends support locking automatically: S3 with a lockfile or DynamoDB, Terraform Cloud, GCS, and Azure Blob all handle it transparently. If an apply is killed mid-run (CI cancelled, laptop crashed), the lock can be left dangling. terraform force-unlock <LOCK_ID> clears it — but only run this once you are certain no operation is actually still in flight, or you reintroduce the exact corruption locking was meant to prevent. -lock-timeout=5m makes a command wait for an existing lock to release instead of failing immediately, which smooths out CI pipelines where runs occasionally overlap. Never disable locking (-lock=false) in shared environments.',
    example: {
      scenario: 'A CI pipeline tries to apply while an engineer is already applying the same stack from their laptop.',
      action: 'The CI run fails immediately with a lock error identifying who holds the lock, preventing concurrent writes to state.',
      code: '$ terraform apply\n\nError: Error acquiring the state lock\n\nLock Info:\n  ID:        9f3c1a2b-...\n  Path:      acme-tfstate-prod/network/terraform.tfstate\n  Operation: OperationTypeApply\n  Who:       dana@laptop\n  Created:   2026-06-05 14:22:10 UTC\n\n# Once you confirm no apply is running:\n$ terraform force-unlock 9f3c1a2b-...',
    },
    responsibilities: [
      'Acquire an exclusive lock before any state-modifying operation',
      'Reject concurrent applies with a clear "who holds it" message',
      'Release the lock automatically when the operation completes',
      'Support force-unlock for recovering from killed runs',
      'Protect state integrity in every shared and automated environment',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/language/state/locking',
    docLabel: 'State locking documentation',
  },
  'drift-detection': {
    group: 'state-backend',
    name: 'Drift Detection',
    short: 'Finds when reality diverges from Terraform-managed state',
    color: '#9333ea',
    tag: 'OBSERVE',
    intro: 'Drift is when the real infrastructure no longer matches what Terraform recorded in state — usually because someone made a change by hand in the cloud console, or an autoscaler or another tool modified a resource out of band. Terraform detects drift during the refresh phase of every plan: it queries the real platform, compares against state, and shows any differences as changes it would make to bring reality back in line. Catching drift early is what keeps "the code is the truth" actually true.',
    deepDive: 'Run a scheduled, read-only plan (terraform plan -detailed-exitcode, exit code 2 = drift) nightly in CI to surface drift before it compounds. The fix for drift is a decision, not always a re-apply: if the manual change was wrong, apply to revert it; if it was a legitimate improvement, update the HCL to match so the next plan is clean. terraform plan -refresh-only shows drift without proposing config-driven changes, isolating "what changed in the world" from "what changed in my code." The deeper cure is organizational: lock down console write access so the only path to changing infrastructure is a reviewed Terraform PR. Drift you cannot prevent (e.g. cloud-managed tags) can be ignored with lifecycle { ignore_changes }.',
    example: {
      scenario: 'Someone manually changed a security group rule in the AWS console during an incident.',
      action: 'The nightly drift-detection plan flags the out-of-band change. The team decides the manual rule was wrong and applies to restore the declared state.',
      code: '$ terraform plan -detailed-exitcode\n\n  # aws_security_group.api has changed\n  ~ resource "aws_security_group" "api" {\n      ~ ingress {\n          ~ cidr_blocks = [\n              - "0.0.0.0/0",   # added by hand\n              + "10.0.0.0/8",\n            ]\n        }\n    }\n\nNote: Objects have changed outside of Terraform\nExit code: 2   # drift detected',
    },
    responsibilities: [
      'Refresh state against reality at the start of every plan',
      'Surface out-of-band changes as proposed corrections',
      'Run on a schedule to catch drift before it compounds',
      'Separate "world changed" from "code changed" via refresh-only',
      'Inform the choice to revert drift or codify it into HCL',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/cli/commands/plan#planning-modes',
    docLabel: 'Plan refresh & drift',
  },
  'workspaces': {
    group: 'collaboration',
    name: 'Workspaces',
    short: 'Multiple state instances from one configuration',
    color: '#0891b2',
    tag: 'ENVIRONMENTS',
    intro: 'A workspace is a named, separate instance of state for the same configuration. The default workspace is "default"; you can create more (dev, staging, prod) and each keeps its own independent state file while sharing the same .tf code. terraform.workspace exposes the current name inside HCL, so you can vary instance sizes or counts per environment. Workspaces are the lightweight way to run the same stack multiple times without copying code.',
    deepDive: 'There is a long-standing debate about workspaces versus directories for environment separation. CLI workspaces share one backend key prefix and one set of variables, which makes them excellent for ephemeral or near-identical environments (per-PR preview stacks) but risky for prod, where you want maximum blast-radius isolation: a separate backend, separate credentials, separate state bucket. Many mature teams therefore use directory-per-environment (env/prod, env/staging) for the prod boundary and reserve CLI workspaces for short-lived copies. Terraform Cloud workspaces are a different, heavier concept — each is a full managed run environment with its own variables and run history, not just a state slot.',
    example: {
      scenario: 'A team wants a smaller, cheaper instance in dev and a larger one in prod from the same code.',
      action: 'They switch workspaces and key the instance type off terraform.workspace, so each environment gets the right size from identical HCL.',
      code: '$ terraform workspace new dev\n$ terraform workspace new prod\n$ terraform workspace select prod\n\n# In HCL:\nlocals {\n  instance_type = {\n    dev  = "t3.small"\n    prod = "m5.large"\n  }[terraform.workspace]\n}\n\nresource "aws_instance" "api" {\n  instance_type = local.instance_type\n}',
    },
    responsibilities: [
      'Maintain isolated state per named environment from one config',
      'Expose the active workspace name to HCL via terraform.workspace',
      'Enable ephemeral per-PR or per-feature preview environments',
      'Switch context with workspace select without copying code',
      'Complement (not replace) directory isolation for production',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/language/state/workspaces',
    docLabel: 'Workspaces documentation',
  },
  'terraform-cloud': {
    group: 'collaboration',
    name: 'Terraform Cloud / HCP',
    short: 'Managed remote runs, state, and collaboration platform',
    color: '#7b42bc',
    tag: 'PLATFORM',
    intro: 'HCP Terraform (formerly Terraform Cloud) is HashiCorp\'s managed platform for running Terraform as a team. Instead of applying from laptops, plans and applies run on remote workers in a consistent environment. It provides managed encrypted state with locking and history, a UI to review plans, variable and secret storage, VCS integration that triggers runs on PRs, and policy enforcement. It turns Terraform from a local CLI tool into a governed, auditable workflow with a clear record of who changed what and when.',
    deepDive: 'Remote runs solve the "works on my machine" problem for infrastructure: the same provider versions, the same credentials (injected as workspace variables, never on a laptop), the same environment every time. VCS-driven workspaces run a speculative plan on every PR and post the result for review, then apply on merge — infrastructure changes flow through the exact same review gate as code. Sentinel and OPA policy sets enforce guardrails before apply (see Policy as Code). The self-hosted equivalent is Terraform Enterprise. Agents let HCP Terraform reach into private networks that have no inbound internet access. For teams, the biggest win is centralized state and a complete audit trail without building that infrastructure yourself.',
    example: {
      scenario: 'A team wants every infrastructure PR to get an automatic plan and require approval before apply.',
      action: 'They configure a cloud block pointing at their HCP organization and workspace. Runs now execute remotely and post plans to PRs automatically.',
      code: 'terraform {\n  cloud {\n    organization = "acme"\n\n    workspaces {\n      name = "network-prod"\n    }\n  }\n}\n\n$ terraform login\n$ terraform init\n# Plans/applies now run remotely in HCP Terraform,\n# with state, locking, variables, and run history managed.',
    },
    responsibilities: [
      'Run plans and applies remotely in a consistent environment',
      'Manage encrypted state, locking, and full run history',
      'Store workspace variables and secrets securely',
      'Trigger speculative plans on PRs via VCS integration',
      'Enforce policy and provide an audit trail of every change',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/cloud-docs',
    docLabel: 'HCP Terraform docs',
  },
  'cicd-integration': {
    group: 'collaboration',
    name: 'CI/CD Integration',
    short: 'Plan on PR, apply on merge — infra as a pipeline',
    color: '#2563eb',
    tag: 'AUTOMATION',
    intro: 'Running Terraform in CI/CD makes infrastructure changes follow the same path as application code: a PR runs terraform plan and posts the diff for review; merging to main runs terraform apply. This removes the "applied from someone\'s laptop with uncommitted changes" failure mode entirely. Tools range from raw GitHub Actions / GitLab CI jobs, to Atlantis (a self-hosted bot that comments plans on PRs and applies on a chat command), to HCP Terraform\'s native VCS runs. The principle is identical: humans review plans, machines apply them.',
    deepDive: 'The golden rule of Terraform in CI is plan and apply the same artifact: terraform plan -out=tfplan in the PR job, persist that file, and terraform apply tfplan in the merge job — so the apply does exactly what was reviewed, with no drift in between. Authenticate with short-lived OIDC tokens (GitHub Actions can assume an AWS role with no stored long-lived keys) rather than static credentials. Gate apply behind branch protection and required reviews. Use -lock-timeout so overlapping runs queue rather than fail. Surface the plan as a PR comment so reviewers do not have to dig through CI logs. Atlantis adds atlantis plan / atlantis apply chat-ops and an apply-lock so two PRs cannot apply the same project concurrently.',
    example: {
      scenario: 'A team wants GitHub Actions to plan on every PR and apply automatically when the PR merges to main.',
      action: 'The workflow authenticates via OIDC (no stored keys), saves a plan artifact on PRs, and applies that exact plan on merge to main.',
      code: 'jobs:\n  terraform:\n    runs-on: ubuntu-latest\n    permissions:\n      id-token: write   # OIDC\n      contents: read\n    steps:\n      - uses: actions/checkout@v4\n      - uses: aws-actions/configure-aws-credentials@v4\n        with:\n          role-to-assume: arn:aws:iam::1234:role/tf-ci\n          aws-region: us-east-1\n      - uses: hashicorp/setup-terraform@v3\n      - run: terraform init\n      - run: terraform plan -out=tfplan\n      - if: github.ref == \'refs/heads/main\'\n        run: terraform apply -auto-approve tfplan',
    },
    responsibilities: [
      'Run terraform plan on every pull request and post the diff',
      'Apply only after review and merge to the main branch',
      'Plan and apply the identical saved plan artifact',
      'Authenticate with short-lived OIDC credentials, not stored keys',
      'Serialize concurrent runs with locking and apply locks',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/tutorials/automation/automate-terraform',
    docLabel: 'Automating Terraform',
  },
  'policy-as-code': {
    group: 'governance',
    name: 'Policy as Code',
    short: 'Automated guardrails that block non-compliant plans',
    color: '#dc2626',
    tag: 'GOVERNANCE',
    intro: 'Policy as code enforces organizational rules on infrastructure automatically, before any apply. Instead of a wiki page saying "always tag resources" or "never open SSH to the world," you write executable policies that inspect the plan and pass or fail it. Sentinel (HashiCorp\'s policy language) and Open Policy Agent (OPA/Rego, used via conftest) are the two main engines. A policy can mandate tags, restrict instance types, forbid public S3 buckets, or require encryption — and the pipeline refuses to apply anything that violates them.',
    deepDive: 'Policies evaluate the machine-readable plan (terraform show -json tfplan) so they reason about exactly what will change, not just the source HCL. Enforcement levels matter: advisory (warn, allow), soft-mandatory (block, but an admin can override with a logged reason), and hard-mandatory (block, no override). Roll new policies out as advisory first to measure their blast radius before turning them mandatory, or you will block every PR on day one. OPA/conftest is the cloud-agnostic, open-source path and runs anywhere; Sentinel is tightly integrated into HCP Terraform with cost-estimation hooks. Pair policy with cost estimation (Infracost) to block changes that exceed a budget threshold. The goal is the same as tests: make the safe thing automatic and the unsafe thing impossible to merge.',
    example: {
      scenario: 'Security mandates that no S3 bucket may ever be created with public-read access.',
      action: 'An OPA policy inspects the JSON plan and denies any aws_s3_bucket with a public ACL. The CI job runs conftest and fails the build on violation.',
      code: '# policy/s3.rego\npackage main\n\ndeny[msg] {\n  r := input.resource_changes[_]\n  r.type == "aws_s3_bucket_acl"\n  r.change.after.acl == "public-read"\n  msg := sprintf(\n    "S3 bucket %s must not be public",\n    [r.address],\n  )\n}\n\n# CI step:\n$ terraform show -json tfplan > plan.json\n$ conftest test plan.json --policy policy/',
    },
    responsibilities: [
      'Encode org rules (tags, encryption, allowed types) as executable policy',
      'Evaluate the machine-readable plan before apply',
      'Block non-compliant changes from ever reaching the platform',
      'Roll out as advisory before promoting to mandatory enforcement',
      'Pair with cost estimation to enforce budget guardrails',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/cloud-docs/policy-enforcement',
    docLabel: 'Policy enforcement',
  },
  'secrets-management': {
    group: 'governance',
    name: 'Secrets Management',
    short: 'Keeping credentials out of code and out of plaintext state',
    color: '#b91c1c',
    tag: 'SECURITY',
    intro: 'Terraform handles secrets in two directions and both are dangerous if done naively. Inputs: never hardcode passwords or API keys in .tf files committed to Git. Outputs: remember that anything a provider returns — generated passwords, private keys, tokens — gets written to state in plaintext. The discipline is to source secrets at runtime from a secrets manager (Vault, AWS Secrets Manager, SSM Parameter Store) and to treat the state file itself as a secret: encrypted at rest, access-controlled, never in a public bucket.',
    deepDive: 'Mark variables and outputs sensitive = true so Terraform redacts them from CLI and plan output (note: this only hides them from logs — they are still stored in state in plaintext). For inputs, prefer pulling from a secrets manager via a data source at plan time, or injecting via TF_VAR_ environment variables in CI rather than committing tfvars with secrets. The cleanest pattern avoids long-lived credentials entirely: use OIDC/workload identity so CI assumes a role with no stored keys at all. Restrict who can read the state backend, because reading state = reading every secret in it. If a secret does leak into state or Git history, rotate it immediately — redaction in logs is not the same as the secret being safe.',
    example: {
      scenario: 'A database module needs a password, and the team refuses to commit it to Git or expose it in plan output.',
      action: 'They read the secret from Vault at plan time via a data source and mark the variable sensitive so it never prints in logs.',
      code: 'data "vault_kv_secret_v2" "db" {\n  mount = "secret"\n  name  = "prod/postgres"\n}\n\nvariable "db_password" {\n  type      = string\n  sensitive = true\n}\n\nresource "aws_db_instance" "main" {\n  username = "app"\n  password = data.vault_kv_secret_v2.db.data["password"]\n}\n# Plan output shows: password = (sensitive value)\n# But it IS still stored in state — protect the backend.',
    },
    responsibilities: [
      'Keep credentials out of committed .tf and tfvars files',
      'Source secrets at runtime from Vault or a cloud secrets manager',
      'Mark sensitive variables and outputs to redact them from logs',
      'Treat the state file as a secret: encrypted and access-controlled',
      'Prefer OIDC/workload identity over long-lived stored keys',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/tutorials/configuration-language/sensitive-variables',
    docLabel: 'Sensitive variables',
  },
  'import-targeting': {
    group: 'governance',
    name: 'Import & Targeting',
    short: 'Adopting existing resources and surgically scoping operations',
    color: '#ea580c',
    tag: 'OPERATIONS',
    intro: 'Two escape hatches handle the messy reality of real infrastructure. Import brings resources that already exist (created by hand, or by another tool) under Terraform management without recreating them — you write the resource block, then tell Terraform which real object it maps to. Targeting (-target) restricts a plan or apply to specific resources, useful for unsticking a partial failure or making a narrow emergency change. Both are powerful and both are easy to misuse, so they come with strong caveats.',
    deepDive: 'Modern Terraform (1.5+) supports declarative import blocks: add an import block with the resource address and real ID, run plan to preview, apply to adopt — and you can even generate the HCL with -generate-config-out. This is far safer than the old imperative terraform import command because the import is code-reviewed and reproducible. Targeting is a sharp tool: -target skips the normal full-graph consistency check, so it can leave state in a half-reconciled state if overused. Treat it as a break-glass operation for recovery, not a routine workflow — if you find yourself targeting constantly, your state is probably too large and should be split. After any targeted apply, always run a full plan to confirm the whole stack is still consistent.',
    example: {
      scenario: 'An S3 bucket was created manually long ago and now needs to be managed by Terraform without downtime.',
      action: 'They write a matching resource block plus an import block referencing the real bucket name, then plan and apply to adopt it into state.',
      code: 'resource "aws_s3_bucket" "legacy" {\n  bucket = "acme-legacy-assets"\n}\n\nimport {\n  to = aws_s3_bucket.legacy\n  id = "acme-legacy-assets"\n}\n\n$ terraform plan\n  # aws_s3_bucket.legacy will be imported\nPlan: 1 to import, 0 to add, 0 to change, 0 to destroy.\n\n$ terraform apply   # now under management, no recreation',
    },
    responsibilities: [
      'Bring pre-existing resources under management without recreating them',
      'Prefer declarative import blocks over imperative import commands',
      'Generate starter HCL for imported resources where supported',
      'Use -target only as a break-glass recovery operation',
      'Always follow a targeted apply with a full consistency plan',
    ],
    docUrl: 'https://developer.hashicorp.com/terraform/language/import',
    docLabel: 'Import documentation',
  },
};

export const TERRAFORM_GROUPS = [
  {
    id: 'authoring',
    title: 'Authoring',
    color: '#7b42bc',
    colorDark: '#5b21b6',
    gradient: 'linear-gradient(135deg, #f3e8ff 0%, #e9d5ff 100%)',
    bg: '#faf5ff',
    components: ['hcl-config', 'providers', 'modules'],
  },
  {
    id: 'core-workflow',
    title: 'Core Workflow',
    color: '#2563eb',
    colorDark: '#1e3a8a',
    gradient: 'linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%)',
    bg: '#eff6ff',
    components: ['terraform-init', 'terraform-plan', 'terraform-apply'],
  },
  {
    id: 'state-backend',
    title: 'State & Backend',
    color: '#059669',
    colorDark: '#064e3b',
    gradient: 'linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%)',
    bg: '#f0fdf4',
    components: ['state-file', 'remote-backend', 'state-locking', 'drift-detection'],
  },
];

export const TERRAFORM_GROUP_COLOR_MAP = {
  'hcl-config':      '#7b42bc',
  'providers':       '#7b42bc',
  'modules':         '#7b42bc',
  'terraform-init':  '#2563eb',
  'terraform-plan':  '#2563eb',
  'terraform-apply': '#2563eb',
  'state-file':      '#059669',
  'remote-backend':  '#059669',
  'state-locking':   '#059669',
  'drift-detection': '#059669',
};

export const TERRAFORM_SECONDARY_SECTIONS = [
  { id: 'collaboration', title: 'Collaboration & Scale', color: '#0891b2', components: ['workspaces', 'terraform-cloud', 'cicd-integration'] },
  { id: 'governance',    title: 'Safety & Governance',   color: '#dc2626', components: ['policy-as-code', 'secrets-management', 'import-targeting'] },
];

export const TERRAFORM_FLOWS = [
  ['hcl-config',      'providers'],
  ['providers',       'modules'],
  ['modules',         'terraform-init'],
  ['terraform-init',  'terraform-plan'],
  ['terraform-plan',  'terraform-apply'],
  ['terraform-apply', 'state-file'],
  ['state-file',      'remote-backend'],
  ['remote-backend',  'state-locking'],
  ['state-locking',   'drift-detection'],
];

export const TERRAFORM_JOURNEY = [
  { title: 'Write HCL',            desc: 'An engineer declares the desired infrastructure in .tf files — resources, variables, and outputs.', actor: 'config' },
  { title: 'terraform init',       desc: 'Downloads providers and modules, configures the backend, and writes the dependency lock file.', actor: 'init' },
  { title: 'terraform plan',       desc: 'Refreshes against the real platform and computes the exact add / change / destroy diff.', actor: 'plan' },
  { title: 'Review the plan',      desc: 'A human (or a PR review) inspects the diff, paying special attention to any replacements.', actor: 'review' },
  { title: 'State lock acquired',  desc: 'The backend locks state so no other apply can run concurrently and corrupt it.', actor: 'lock' },
  { title: 'terraform apply',      desc: 'Provider API calls create, update, and destroy real resources in dependency order.', actor: 'apply' },
  { title: 'State updated',        desc: 'Real resource IDs and attributes are written back to the remote state file.', actor: 'state' },
  { title: 'Lock released',        desc: 'The state lock is released and outputs become available to consumers.', actor: 'outputs' },
  { title: 'Drift detection',      desc: 'A scheduled plan later detects any out-of-band manual changes (drift) over time.', actor: 'drift' },
];

export const TERRAFORM_TOPIC = {
    id: 'terraform',
    name: 'Terraform',
    short: 'Declare infrastructure, plan the diff, apply with confidence.',
    accent: '#7b42bc',
    accentSoft: '#ede9fe',
    eyebrow: 'CloudTruck · Terraform · from HCL to live infrastructure',
    heroTitle: 'Infrastructure as code, revealed.',
    heroLead: 'Trace the full Terraform lifecycle: declarative HCL, providers and modules, init, the plan/apply loop, and the state file that ties config to reality. Then go beyond with workspaces, remote backends, policy as code, and drift detection. Click any component to learn what it does — from first principles to production patterns.',
    heroStats: ['16 components', 'plan / apply / state', 'drift & policy as code'],
    diagramLabel: 'STACK · prod · config-to-state graph',
    panelHint: 'trace the apply flow ->',
    sectionsTitle: 'Collaboration, safety, and governance',
    sectionsLead: 'A production Terraform setup is more than plan and apply. Workspaces, managed runs, CI/CD integration, policy as code, secrets management, and import workflows are the scaffold that keeps infrastructure safe, reviewable, and reproducible across a team.',
    workflowTitle: 'The journey of a terraform apply',
    workflowLead: 'Follow a change from a declarative .tf file to live, recorded infrastructure, through every plan, lock, and state write.',
    workflowCommand: 'terraform apply tfplan',
    workflowRealtimeLabel: 'Live apply trace',
    compareTitle: 'The mental model',
    compareParagraphs: [
      'Terraform is two ideas working together: a declarative description of the infrastructure you want, and a state file recording the infrastructure that actually exists. Every plan is just the diff between those two; every apply reconciles reality toward your declaration.',
      'Once that clicks, the whole tool becomes systematic. Providers translate HCL into API calls, modules package patterns for reuse, the backend makes state shared and safe, and locking plus policy keep concurrent teams from corrupting it. The graph engine and the state machine never change — only the resources flowing through them do.',
    ],
    footerTicker: 'TERRAFORM · HCL · INIT · PLAN · APPLY · STATE · DRIFT',
    emptySearch: 'No Terraform component matched that search.',
    comingSoonCopy: 'This future topic will get the same interactive treatment.',
    components: TERRAFORM_COMPONENTS,
    groups: TERRAFORM_GROUPS,
    groupColorMap: TERRAFORM_GROUP_COLOR_MAP,
    secondarySections: TERRAFORM_SECONDARY_SECTIONS,
    flows: TERRAFORM_FLOWS,
    journey: TERRAFORM_JOURNEY,
};

export default TERRAFORM_TOPIC;
