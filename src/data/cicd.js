/* Cicd Topic Data — CloudTruck */

export const CICD_COMPONENTS = {
  'dev-push': {
    group: 'source-control',
    name: 'Developer Push',
    short: 'The human event that starts every pipeline run',
    color: '#0f172a',
    tag: 'SOURCE',
    intro: 'Every CI/CD pipeline starts with a single human action: pushing code to a branch, merging a pull request, or tagging a release. This is not a system component — it is the trigger. The moment git push lands on the remote, it fires a webhook that wakes up the rest of the pipeline. Understanding this boundary is critical: pipelines are event-driven, not time-driven. No push = no pipeline = nothing changes in production.',
    deepDive: 'Teams structure their pipelines around what kinds of pushes should trigger which workflows. A push to a feature branch might trigger only unit tests. A merge to main triggers a full build + deploy to staging. A semver tag like v1.4.0 triggers the full release pipeline including production approval gates. Branch protection rules at the Git provider level enforce that no code lands on main without passing CI — making the push event the gatekeeper of quality. GitOps teams go further: the only path to production is a PR merge; direct pushes to main are blocked entirely.',
    example: {
      scenario: 'A backend engineer merges a pull request into main after code review approval.',
      action: 'The merge commit triggers a push event on the GitHub webhook, which fires the ci.yml workflow with ref=refs/heads/main.',
      code: '$ git checkout main\n$ git merge feature/payment-retry --no-ff\n$ git push origin main\n\n# GitHub receives push event\n# Webhook payload → Actions runner\n# Triggered: .github/workflows/ci.yml\n# Event: push, ref: refs/heads/main\n# SHA: d4f3e9a7b2c1...',
    },
    responsibilities: [
      'Author and commit code changes to feature branches',
      'Create pull requests and participate in code review',
      'Tag releases using semver conventions (v1.2.3)',
      'Respect branch protection rules and required status checks',
      'Never push directly to main — all changes go through PRs',
    ],
    docUrl: 'https://docs.github.com/en/actions/writing-workflows/choosing-when-your-workflow-runs/events-that-trigger-workflows',
    docLabel: 'Events that trigger GitHub Actions',
  },
  'git-repo': {
    group: 'source-control',
    name: 'Git Repository',
    short: 'The canonical source of truth for code and pipeline config',
    color: '#f97316',
    tag: 'SOURCE CONTROL',
    intro: 'The Git repository holds two things equally important: the application source code and the pipeline definition files (.github/workflows/, .gitlab-ci.yml, Jenkinsfile). Pipeline config lives alongside the code it builds — this is infrastructure-as-code for automation. A PR review is simultaneously a review of the application logic and the deployment procedure that will ship it. Change the pipeline? Open a PR like any other code change.',
    deepDive: 'Storing pipeline definitions in the repo (not in a GUI) gives teams diff history, rollback capability, and code review for every workflow change. Branch protection rules, CODEOWNERS files, and environment secrets scopes gate which branches can deploy where. Monorepo strategies complicate this further: path filtering (on: push: paths:) ensures only the affected service\'s pipeline runs when its files change. Reusable workflows (workflow_call) let platform teams define security and compliance steps once and have every service call them — a single PR to the shared workflow is a fleet-wide security update.',
    example: {
      scenario: 'A platform team adds a required security scan to all services in a monorepo.',
      action: 'They add a reusable workflow file and update each service workflow to call it. One PR, reviewed once, applies security to every service.',
      code: '# .github/workflows/security.yml (reusable)\non:\n  workflow_call:\njobs:\n  sast:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: Run SAST\n        run: semgrep --config=auto src/\n\n# Each service calls it:\njobs:\n  security:\n    uses: org/.github/.github/workflows/security.yml@main',
    },
    responsibilities: [
      'Host application source code with full version history',
      'Store pipeline definition files alongside the code they build',
      'Enforce branch protection: required reviews, passing checks, signed commits',
      'Provide webhook events to downstream CI systems on push, PR, and tag events',
      'Host environment configuration and deployment manifests',
    ],
    docUrl: 'https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches',
    docLabel: 'Branch protection rules',
  },
  'webhook': {
    group: 'source-control',
    name: 'Webhook Trigger',
    short: 'HTTP event that wakes up the CI system instantly',
    color: '#7c3aed',
    tag: 'EVENT',
    intro: 'A webhook is an HTTP POST the Git provider sends to the CI system the moment a triggering event occurs. It carries a JSON payload describing what happened: the ref, commit SHA, author, and changed files. The CI system verifies the HMAC signature, parses the payload, and decides whether any pipeline definitions match this event. This is the bridge between source control and automation — zero polling, instant response.',
    deepDive: 'Webhook delivery is best-effort with retry, not guaranteed exactly-once. If the CI endpoint is down, GitHub queues retries with exponential backoff for up to several hours before giving up. CI systems must be idempotent for re-delivered events. The payload enables conditional logic: pipelines inspect github.event.pull_request.merged, github.ref, and github.event.commits[*].modified to decide which jobs to run. Securing webhooks with HMAC-SHA256 prevents external actors from spoofing pipeline triggers. Enterprise setups use IP allowlists and mTLS in addition to HMAC for defense in depth.',
    example: {
      scenario: 'A developer pushes tag v2.1.0. GitHub fires a push event webhook to the CI endpoint.',
      action: 'The CI system verifies the HMAC signature, sees ref=refs/tags/v2.1.0, and triggers the release workflow (not the regular CI workflow).',
      code: 'POST /hooks/github HTTP/1.1\nX-GitHub-Event: push\nX-Hub-Signature-256: sha256=abc123def456...\nContent-Type: application/json\n\n{\n  "ref": "refs/tags/v2.1.0",\n  "head_commit": {\n    "id": "d4f3e9a7b2c1...",\n    "message": "chore: release v2.1.0"\n  },\n  "repository": { "full_name": "org/api-service" }\n}',
    },
    responsibilities: [
      'Carry event payload (ref, SHA, author, changed files) from Git to CI',
      'Allow the CI system to filter and match pipeline definitions to events',
      'Provide HMAC-SHA256 signature for authentication',
      'Support retry delivery with backoff for transient failures',
    ],
    docUrl: 'https://docs.github.com/en/webhooks/about-webhooks',
    docLabel: 'About GitHub webhooks',
  },
  'ci-runner': {
    group: 'build-test',
    name: 'CI Runner',
    short: 'The compute that executes every pipeline job',
    color: '#2563eb',
    tag: 'COMPUTE',
    intro: 'The runner is the machine — a VM, container, or bare-metal server — that actually runs your pipeline jobs. GitHub Actions provides hosted runners (ubuntu-latest, macos-latest, windows-latest) that spin up fresh for every job. Self-hosted runners are machines you manage that register with the CI system and poll for work. The runner clones your repo, sets up the environment, and executes steps one by one in isolation.',
    deepDive: 'Hosted runners are ephemeral: a clean VM for every job eliminates state leakage but adds startup time (15–40 seconds). Self-hosted runners persist between jobs, which is faster but requires explicit cleanup of artifacts, credentials, and temporary files to avoid contamination between runs. Organizations with GPU workloads, air-gapped deployments, or exotic OS targets must use self-hosted runners. Matrix builds run multiple variants in parallel — Node 18/20/22, Python 3.11/3.12, Linux/Windows — dramatically cutting wall-clock test time without changing the pipeline logic.',
    example: {
      scenario: 'A matrix build tests the API against Node 18, 20, and 22 simultaneously.',
      action: 'Three GitHub Actions runner VMs pick up the three jobs in parallel, each in a fresh ubuntu-latest environment, cutting total test time by 3×.',
      code: 'jobs:\n  test:\n    strategy:\n      matrix:\n        node: [18, 20, 22]\n      fail-fast: false\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ matrix.node }}\n          cache: "npm"\n      - run: npm ci\n      - run: npm test -- --coverage',
    },
    responsibilities: [
      'Poll or receive job assignments from the CI coordinator',
      'Clone the repository at the exact target commit SHA',
      'Execute pipeline steps in order inside an isolated, reproducible environment',
      'Report step status, logs, and artifacts back to the CI system',
      'Clean up the workspace after the job completes',
    ],
    docUrl: 'https://docs.github.com/en/actions/using-github-hosted-runners/using-github-hosted-runners/about-github-hosted-runners',
    docLabel: 'About GitHub-hosted runners',
  },
  'build-stage': {
    group: 'build-test',
    name: 'Build Stage',
    short: 'Compile, package, and produce a reproducible deployable artifact',
    color: '#d97706',
    tag: 'BUILD',
    intro: 'The build stage transforms source code into a deployable artifact. For Node.js: npm run build. For Go: go build. For a containerized app: docker build. The output should be reproducible — given the same inputs (commit SHA, pinned dependencies, build args), the same artifact emerges every time. Build caching is the main lever for speed: avoiding redundant compilation and Docker layer rebuilds can cut build time from 8 minutes to under 1 minute.',
    deepDive: 'Reproducible builds are harder than they sound. Non-determinism creeps in via timestamps embedded in binaries, package-lock files that allow minor version drift, or apt-get pulling a different package version on different days. Production pipelines pin every dependency, use content-addressable caches (BuildKit cache, GitHub Actions cache keyed on lock file hash), and produce image digests for downstream verification. Docker multi-stage builds keep the final image lean by discarding compilers, build tools, and test fixtures before shipping — a 900 MB build image becomes a 50 MB production image.',
    example: {
      scenario: 'A Python service needs a Docker image built and pushed to the registry after every merge to main.',
      action: 'The build stage runs docker buildx with BuildKit cache, embeds the git SHA for traceability, then pushes both the SHA tag (immutable) and latest (convenience).',
      code: '- name: Build and push\n  run: |\n    docker buildx build \\\n      --build-arg GIT_SHA=${{ github.sha }} \\\n      --cache-from type=registry,ref=ghcr.io/org/api:cache \\\n      --cache-to   type=registry,ref=ghcr.io/org/api:cache,mode=max \\\n      --tag ghcr.io/org/api:${{ github.sha }} \\\n      --tag ghcr.io/org/api:latest \\\n      --push \\\n      .\n\n# Verify the digest\ndocker inspect ghcr.io/org/api:${{ github.sha }} \\\n  --format "{{.RepoDigests}}"',
    },
    responsibilities: [
      'Compile source code and resolve all dependencies from pinned lock files',
      'Run docker build to produce an OCI-compliant container image',
      'Tag artifacts with the exact commit SHA and semantic version',
      'Push images to the artifact registry after successful build',
      'Fail fast on any compilation error before wasting time on tests',
    ],
    docUrl: 'https://docs.docker.com/build/cache/',
    docLabel: 'Docker BuildKit cache guide',
  },
  'test-stage': {
    group: 'build-test',
    name: 'Test Stage',
    short: 'Gate every deployment behind automated quality checks',
    color: '#16a34a',
    tag: 'QUALITY',
    intro: 'The test stage runs automated checks that verify the artifact is safe to ship. It includes: unit tests (fast, no external deps, milliseconds each), integration tests (require a real database or queue), end-to-end tests (full browser or API flows), and coverage enforcement. A failing test stage blocks the pipeline cold — nothing reaches the registry, nothing reaches staging, nothing reaches production. This is the main developer feedback loop: failures surface within minutes of a push.',
    deepDive: 'Test strategy design follows the testing pyramid: many unit tests, fewer integration tests, fewest E2E tests. Teams that invert this pyramid — heavy on slow E2E — end up with 40-minute pipelines that discourage frequent merging. Coverage thresholds (--coverage-threshold flags) prevent regression. For microservices, contract testing (Pact) validates that two services remain API-compatible without standing up a full integration environment. Test parallelism (splitting test files across runners) is the fastest way to cut CI time when all other optimizations are exhausted.',
    example: {
      scenario: 'A PR adds a discount calculation function. The test stage must pass before the PR can merge.',
      action: 'Jest runs unit tests in under 2 minutes with 80% coverage enforced. A parallel integration job spins up a Postgres container and tests the discount API endpoint end-to-end.',
      code: 'jobs:\n  unit:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: npm ci\n      - run: |\n          npm test -- \\\n            --coverage \\\n            --coverageThreshold=\'{"global":{"lines":80}}\'\n\n  integration:\n    runs-on: ubuntu-latest\n    services:\n      postgres:\n        image: postgres:16\n        env: { POSTGRES_PASSWORD: test }\n        options: --health-cmd pg_isready\n    steps:\n      - uses: actions/checkout@v4\n      - run: npm ci\n      - run: npm run test:integration',
    },
    responsibilities: [
      'Run unit tests and enforce code coverage thresholds',
      'Spin up real service dependencies (databases, queues) for integration tests',
      'Run SAST, secret scanning, and dependency CVE audits',
      'Fail the pipeline and block deployment on any test failure',
      'Publish test results and coverage reports to the PR as status checks',
    ],
    docUrl: 'https://docs.github.com/en/actions/use-cases-and-examples/building-and-testing/building-and-testing-nodejs',
    docLabel: 'Building and testing Node.js',
  },
  'artifact-registry': {
    group: 'release',
    name: 'Artifact Registry',
    short: 'Immutable, versioned storage for every tested build artifact',
    color: '#0ea5e9',
    tag: 'REGISTRY',
    intro: 'The artifact registry stores everything that passes build and test: Docker images, npm packages, Maven JARs, Helm charts, raw binaries. Artifacts are immutable — once pushed with a SHA tag, that tag must never be overwritten. The registry is the handoff point between CI (which builds) and CD (which deploys). A deployment references an exact artifact digest, ensuring that what was tested is exactly what runs in production. No surprises.',
    deepDive: 'Registry options: Docker Hub (public), GitHub Container Registry (ghcr.io, integrated with Actions permissions), AWS ECR, Google Artifact Registry, and self-hosted Harbor. Immutability policies prevent tag mutation — configure these on day one before someone accidentally overwrites a release tag. Retention policies auto-delete untagged images to control storage costs. Vulnerability scanning (Trivy, Grype, Snyk Container) runs against every pushed image and can gate Kubernetes deployments via admission controllers. Image signing with Cosign + Sigstore creates a verifiable chain from source commit SHA to deployed container, enabling supply-chain security.',
    example: {
      scenario: 'After a successful build and test run, the pipeline pushes the Docker image to GitHub Container Registry.',
      action: 'The image is tagged with the git SHA (immutable reference deployments use) and latest (mutable convenience tag for local dev). The digest is saved as a workflow output.',
      code: '- name: Log in to GHCR\n  run: |\n    echo ${{ secrets.GITHUB_TOKEN }} | \\\n      docker login ghcr.io \\\n        -u ${{ github.actor }} \\\n        --password-stdin\n\n- name: Push image\n  id: push\n  run: |\n    docker push ghcr.io/org/api:${{ github.sha }}\n    docker push ghcr.io/org/api:latest\n    DIGEST=$(docker inspect \\\n      ghcr.io/org/api:${{ github.sha }} \\\n      --format "{{index .RepoDigests 0}}")\n    echo "digest=$DIGEST" >> $GITHUB_OUTPUT',
    },
    responsibilities: [
      'Store Docker images and build artifacts with content-addressable SHA digests',
      'Enforce immutability: SHA tags must never be overwritten after push',
      'Run automated vulnerability scans against every pushed image',
      'Control access: CI has push rights, production infra has pull-only rights',
      'Retain tagged releases; auto-delete old untagged images to control costs',
    ],
    docUrl: 'https://docs.github.com/en/packages/working-with-a-github-packages-registry/working-with-the-container-registry',
    docLabel: 'GitHub Container Registry',
  },
  'staging-deploy': {
    group: 'release',
    name: 'Staging Deploy',
    short: 'Automatic deployment to a production-equivalent environment',
    color: '#8b5cf6',
    tag: 'DEPLOY',
    intro: 'Staging is the environment that mirrors production as closely as possible and receives every successful build from main. It is the final automated validation gate: does the artifact actually run correctly in a real environment, with real configuration, against real downstream services? Smoke tests and synthetic monitoring run against staging after every deploy to confirm the service is alive and behaviorally correct before a human ever sees the approval request.',
    deepDive: 'The fidelity gap between staging and production is the root cause of most "but it worked in staging" incidents. Minimize this gap: use identical Kubernetes manifests (differ only in replica count and resource limits), point staging at a production-schema replica database, and run the same secret rotation procedures. Progressive delivery to staging — canary 10% of staging traffic to the new version — gives you an additional signal before approving production. Environment-specific secrets live in the CD system (Argo CD, Flux) or a secrets manager (Vault, AWS Secrets Manager), never in the pipeline YAML.',
    example: {
      scenario: 'Every merge to main auto-deploys the new image to the staging Kubernetes cluster and runs a smoke test.',
      action: 'The pipeline patches the Deployment image tag to the new SHA and waits for rollout to complete before running health checks.',
      code: '- name: Deploy to staging\n  run: |\n    kubectl set image deployment/api \\\n      api=ghcr.io/org/api:${{ github.sha }} \\\n      --namespace staging\n    kubectl rollout status deployment/api \\\n      --namespace staging \\\n      --timeout=120s\n\n- name: Smoke test\n  run: |\n    curl -f https://staging.api.example.com/healthz\n    curl -f https://staging.api.example.com/readyz',
    },
    responsibilities: [
      'Automatically deploy the artifact to a production-equivalent environment',
      'Run smoke tests and health checks after every deploy',
      'Serve as the final automated gate before human review for production',
      'Provide a realistic environment for exploratory testing and stakeholder demos',
    ],
    docUrl: 'https://docs.github.com/en/actions/managing-workflow-runs-and-deployments/managing-deployments/managing-environments-for-deployment',
    docLabel: 'GitHub deployment environments',
    troubleshooting: {
      symptom: 'Staging deploy completes but smoke test returns 502. Production release is blocked.',
      checks: [
        'kubectl rollout status deployment/api -n staging — did all pods come up?',
        'kubectl logs -l app=api -n staging --tail=50 — look for startup errors',
        'Verify the image SHA in the running pod matches what was pushed',
        'Check if the new version requires a DB schema migration that was not run',
      ],
      fix: 'Add a pre-deploy migration job to the pipeline that runs before kubectl set image. Use kubectl wait to block until the migration Job completes successfully.',
      code: '- name: Run DB migration\n  run: |\n    kubectl apply -f - <<EOF\n    apiVersion: batch/v1\n    kind: Job\n    metadata:\n      name: migrate-${{ github.sha }}\n      namespace: staging\n    spec:\n      template:\n        spec:\n          containers:\n          - name: migrate\n            image: ghcr.io/org/api:${{ github.sha }}\n            command: ["python", "manage.py", "migrate"]\n          restartPolicy: Never\n    EOF\n    kubectl wait job/migrate-${{ github.sha }} \\\n      --for=condition=complete \\\n      --namespace staging \\\n      --timeout=120s',
    },
  },
  'approval-gate': {
    group: 'release',
    name: 'Approval Gate',
    short: 'Mandatory human checkpoint before any production deployment',
    color: '#dc2626',
    tag: 'GOVERNANCE',
    intro: 'The approval gate is a mandatory human pause between staging validation and production deployment. It gives an engineer, tech lead, or release manager the chance to review what is about to go to production: the list of commits, the artifact digest, staging test results, and any open incidents. In GitHub Actions this is an Environment Protection Rule requiring one or more designated reviewers to approve before the production environment can be targeted. Reject = deployment cancelled with a reason logged.',
    deepDive: 'Approval gates create an immutable audit trail. Every production deployment records who approved it, when, and which artifact digest was approved — essential evidence for SOC 2, ISO 27001, and PCI DSS change control. Some teams add time-window restrictions: no production deploys between Friday 3pm and Monday 9am. High-velocity teams use automated approval bots that approve if all checks pass AND no incidents are open in PagerDuty — preserving the audit record while removing human bottlenecks for routine releases. Every rejection is logged with a reason, providing a paper trail for post-incident reviews.',
    example: {
      scenario: 'After staging smoke tests pass, the pipeline pauses and sends a Slack notification requesting production approval.',
      action: 'A senior engineer reviews the commit list and approves via the GitHub Environments UI. The pipeline resumes the production deploy job.',
      code: '# Workflow definition\njobs:\n  deploy-prod:\n    needs: [deploy-staging, smoke-test]\n    environment:\n      name: production        # ← triggers approval requirement\n      url: https://api.example.com\n    steps:\n      - name: Deploy to production\n        run: |\n          kubectl set image deployment/api \\\n            api=ghcr.io/org/api:${{ github.sha }} \\\n            --namespace prod\n\n# In GitHub: Settings → Environments → production\n# Required reviewers: @senior-engineers team\n# Wait timer: 0 minutes (immediate on approval)',
    },
    responsibilities: [
      'Pause the pipeline between staging and production until a human approves',
      'Notify designated reviewers via Slack, email, or GitHub UI',
      'Record approver identity, timestamp, and artifact digest for the audit log',
      'Allow rejection with a mandatory reason that cancels the deployment',
      'Optionally enforce time-window restrictions (no deploys on Friday evenings)',
    ],
    docUrl: 'https://docs.github.com/en/actions/managing-workflow-runs-and-deployments/managing-deployments/reviewing-deployments',
    docLabel: 'Reviewing deployments',
  },
  'prod-deploy': {
    group: 'release',
    name: 'Production Deploy',
    short: 'Controlled, observable rollout to live production with automatic rollback',
    color: '#059669',
    tag: 'PRODUCTION',
    intro: 'The production deployment step applies the approved artifact to the live environment serving real users. Modern strategies avoid big-bang cutover: rolling updates replace old pods gradually with readiness probes guarding against bad traffic. Blue-green deployments maintain a parallel environment and swap traffic atomically. Canary deployments route a small percentage of traffic to the new version and ramp up only if error rates stay healthy. Automatic rollback is the safety net that makes fast deploys safe.',
    deepDive: 'Automated rollback is what separates mature pipelines from dangerous ones. A post-deploy health check watches error rate and p99 latency for 5–10 minutes against SLO thresholds. If either spikes, the pipeline re-runs the deploy job with the previous SHA — rollback is just re-deploying a known-good immutable artifact. GitOps operators (Argo CD, Flux) extend this: if anyone manually patches a production resource, the operator detects the drift within seconds and reverts to the Git-declared state. The deployment event should also create a record in PagerDuty, your changelog, and your DORA metrics collector so you can measure deployment frequency and change failure rate over time.',
    example: {
      scenario: 'A canary deployment routes 10% of production traffic to the new API version. After 10 minutes of healthy metrics it ramps to 100%.',
      action: 'Argo Rollouts manages the traffic split using Istio weights, monitors error rate via Prometheus, and auto-promotes if the error rate stays under 0.1%.',
      code: 'apiVersion: argoproj.io/v1alpha1\nkind: Rollout\nmetadata:\n  name: api\n  namespace: prod\nspec:\n  strategy:\n    canary:\n      steps:\n        - setWeight: 10\n        - pause: {duration: 10m}\n        - analysis:\n            templates:\n              - templateName: success-rate\n        - setWeight: 50\n        - pause: {duration: 5m}\n        - setWeight: 100\n  template:\n    spec:\n      containers:\n        - name: api\n          image: ghcr.io/org/api:v2.1.0',
    },
    responsibilities: [
      'Apply the approved artifact to the production environment',
      'Execute the deployment strategy: rolling, blue-green, or canary',
      'Run post-deploy health checks and monitor SLO metrics for 5–10 minutes',
      'Trigger automatic rollback if error rate or latency breaches the SLO threshold',
      'Record the deployment event in observability, changelog, and DORA metric systems',
    ],
    docUrl: 'https://argo-rollouts.readthedocs.io/en/stable/concepts/',
    docLabel: 'Argo Rollouts deployment strategies',
    troubleshooting: {
      symptom: 'Production deploy completes but error rate spikes to 5% within 2 minutes of rollout.',
      checks: [
        'kubectl logs -l app=api -n prod --since=5m — look for panic/exception patterns',
        'Compare error traces to the set of commits included in this release',
        'Check if the release adds a NOT NULL column without a default to a populated table',
        'Verify downstream service credentials and health (external APIs, databases)',
      ],
      fix: 'Trigger rollback immediately by re-deploying the previous SHA. Fix forward in a new PR with a regression test that catches the specific failure.',
      code: '# Option 1: kubectl rollback\nkubectl rollout undo deployment/api \\\n  --namespace prod\nkubectl rollout status deployment/api \\\n  --namespace prod\n\n# Option 2: redeploy previous SHA explicitly\nkubectl set image deployment/api \\\n  api=ghcr.io/org/api:$PREVIOUS_SHA \\\n  --namespace prod\n\n# Option 3: Argo Rollouts abort\nargocd app rollback api-prod \\\n  --revision $PREVIOUS_REVISION',
    },
  },

  // Secondary — Security & Compliance
  'secret-scanning': {
    group: 'security',
    name: 'Secret Scanning',
    short: 'Detect credentials committed to the repo before they reach production',
    color: '#dc2626',
    tag: 'SECURITY',
    intro: 'Secret scanning automatically detects accidentally committed API keys, database passwords, tokens, and private keys in every push and PR. GitHub Advanced Security scans every commit; tools like TruffleHog and gitleaks run as pre-commit hooks or CI steps. Finding a leaked secret in CI is orders of magnitude cheaper than finding it after it has been rotated by an attacker. The fix is always the same: rotate the credential immediately, then remove it from history.',
    deepDive: 'GitHub\'s secret scanning covers 200+ token patterns from major providers (AWS, GCP, Stripe, Twilio, etc.) and alerts the provider directly when a match is found — AWS can auto-revoke a leaked key before you even see the alert. Push protection blocks the push at the moment of git push if a secret is detected, preventing it from ever entering the repo history. For custom internal tokens, define your own patterns using regular expressions in the Advanced Security settings.',
    example: {
      scenario: 'A developer accidentally commits a Stripe live API key in a config file.',
      action: 'GitHub secret scanning detects the Stripe pattern, blocks the push (with push protection enabled), and notifies both the developer and the Stripe security team to auto-revoke the key.',
      code: '# .github/workflows/secret-scan.yml\njobs:\n  secret-scan:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0\n      - name: Run TruffleHog\n        run: |\n          docker run --rm \\\n            trufflesecurity/trufflehog:latest \\\n            github \\\n            --repo https://github.com/org/repo \\\n            --since-commit HEAD~1 \\\n            --fail',
    },
    responsibilities: [
      'Scan every commit and PR diff for known credential patterns',
      'Block pushes containing live credentials (with push protection)',
      'Alert developers and credential owners on detection',
      'Maintain audit log of detected and resolved secrets',
    ],
    docUrl: 'https://docs.github.com/en/code-security/secret-scanning/introduction/about-secret-scanning',
    docLabel: 'About secret scanning',
  },
  'sast': {
    group: 'security',
    name: 'SAST',
    short: 'Static code analysis that finds security bugs before runtime',
    color: '#dc2626',
    tag: 'SECURITY',
    intro: 'Static Application Security Testing (SAST) analyzes source code without executing it, looking for vulnerability patterns: SQL injection, XSS, insecure deserialization, hardcoded credentials, unsafe dependencies. Tools like Semgrep, CodeQL, SonarQube, and Snyk Code run as CI jobs on every PR. A finding blocks the merge until the vulnerability is fixed or explicitly accepted with a risk justification. SAST catches the class of bugs that code review misses because reviewers focus on logic, not security patterns.',
    deepDive: 'CodeQL (GitHub\'s SAST engine) builds a semantic model of the code and queries it for vulnerability patterns — it understands data flow, so it can trace a user-controlled input from an HTTP parameter through multiple function calls to an unsanitized SQL query. Semgrep is faster and easier to customize: teams write their own rules to enforce internal coding standards alongside the community ruleset. False positive management is the main pain point: a rule that fires on every PR for a non-issue trains developers to ignore it. Tune rules to eliminate noise before mandating them as merge blockers.',
    example: {
      scenario: 'A new API endpoint processes user input without validation. SAST detects a potential SQL injection.',
      action: 'CodeQL traces the unsanitized req.query.id parameter from the Express route handler directly into a raw SQL string interpolation and fails the PR check.',
      code: '# .github/workflows/codeql.yml\njobs:\n  analyze:\n    runs-on: ubuntu-latest\n    permissions:\n      security-events: write\n    steps:\n      - uses: actions/checkout@v4\n      - uses: github/codeql-action/init@v3\n        with:\n          languages: javascript\n      - uses: github/codeql-action/analyze@v3\n\n# Finding:\n# app.get("/user", (req, res) => {\n#   db.query(`SELECT * FROM users\n#     WHERE id = ${req.query.id}`)  ← SQL injection\n# })',
    },
    responsibilities: [
      'Scan source code for OWASP Top 10 vulnerability patterns on every PR',
      'Block merges on high-severity findings',
      'Track vulnerability trends and regressions over time',
      'Provide developer-friendly remediation guidance inline in the PR',
    ],
    docUrl: 'https://codeql.github.com/',
    docLabel: 'CodeQL documentation',
  },
  'dependency-audit': {
    group: 'security',
    name: 'Dependency Audit',
    short: 'Catch vulnerable third-party packages before they reach production',
    color: '#dc2626',
    tag: 'SECURITY',
    intro: 'Dependency auditing scans your package manifests (package.json, requirements.txt, go.mod, pom.xml) against CVE databases and flags packages with known vulnerabilities. Tools: npm audit, pip-audit, Snyk, Dependabot, OWASP Dependency-Check. Running this in CI ensures you never unknowingly ship a critical CVE. Dependabot goes further by automatically opening PRs to upgrade vulnerable packages as new CVEs are published.',
    deepDive: 'The log4shell vulnerability (CVE-2021-44228) taught the industry that a single transitive dependency three levels deep can compromise every Java application on the internet. Modern dependency auditing handles transitive deps: it builds the full dependency tree and checks every node. Lock files (package-lock.json, poetry.lock) are essential — without them, npm install on a CI runner could pull a different version than your dev machine. Renovate and Dependabot keep lock files fresh automatically, preventing the "dependency debt" that makes major version upgrades painful.',
    example: {
      scenario: 'A new CVE is published for a direct dependency with CVSS score 9.8. Dependabot opens a PR.',
      action: 'The CI pipeline blocks merging any code that introduces a new critical-severity CVE. Dependabot\'s automated PR includes the patch version and passes all tests.',
      code: '# .github/workflows/audit.yml\njobs:\n  audit:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: npm ci\n      - name: Audit production deps\n        run: |\n          npm audit \\\n            --audit-level=critical \\\n            --omit=dev\n\n# .github/dependabot.yml\nversion: 2\nupdates:\n  - package-ecosystem: npm\n    directory: "/"\n    schedule:\n      interval: weekly\n    open-pull-requests-limit: 10',
    },
    responsibilities: [
      'Scan all direct and transitive dependencies against CVE databases on every PR',
      'Block merges that introduce critical or high-severity vulnerabilities',
      'Auto-open upgrade PRs via Dependabot or Renovate when CVEs are published',
      'Generate a Software Bill of Materials (SBOM) for each release',
    ],
    docUrl: 'https://docs.github.com/en/code-security/dependabot/dependabot-alerts/about-dependabot-alerts',
    docLabel: 'Dependabot alerts',
  },

  // Secondary — Pipeline Observability
  'pipeline-metrics': {
    group: 'observability',
    name: 'Pipeline Metrics',
    short: 'Measure pipeline health and DORA metrics to improve engineering velocity',
    color: '#7c3aed',
    tag: 'OBSERVABILITY',
    intro: 'Pipeline metrics track the health and performance of your CI/CD system itself. Key measurements: pipeline duration (how long does a full run take?), queue time (how long do jobs wait for a runner?), success rate (what % of runs pass?), and the four DORA metrics: deployment frequency, lead time for changes, change failure rate, and mean time to recover. These numbers are the engineering team\'s health report.',
    deepDive: 'DORA (DevOps Research and Assessment) metrics are the industry standard for measuring software delivery performance. Elite performers deploy multiple times per day with lead times under an hour and recover from failures in under an hour. Tracking these over time reveals the impact of process changes — did adding the new security scan increase lead time? Did splitting the monolith reduce change failure rate? Tools: LinearB, Sleuth, Faros AI, Jellyfish, or a custom Grafana dashboard built on your CI provider\'s API. The metric that matters most is whichever one your team currently ignores.',
    example: {
      scenario: 'The platform team notices P99 build time has doubled over 6 months as the codebase grew.',
      action: 'A Grafana dashboard tracking job-level durations reveals the Docker build step grew from 2 to 8 minutes due to missing cache configuration. Adding a BuildKit registry cache drops it back to 90 seconds.',
      code: '# Example: query GitHub Actions metrics via API\ncurl -H "Authorization: token $GH_TOKEN" \\\n  "https://api.github.com/repos/org/api/actions/runs?per_page=100" \\\n  | jq \'[.workflow_runs[] | {\n      id: .id,\n      conclusion: .conclusion,\n      duration: (.updated_at | fromdateiso8601) -\n                (.created_at | fromdateiso8601)\n    }]\'\n\n# DORA lead time = time from first commit to production deploy',
    },
    responsibilities: [
      'Track pipeline duration, queue time, and success rate over time',
      'Measure DORA metrics: deployment frequency, lead time, failure rate, MTTR',
      'Alert on anomalies: sudden increase in failure rate or duration',
      'Provide dashboards that make engineering bottlenecks visible to the team',
    ],
    docUrl: 'https://cloud.google.com/blog/products/devops-sre/using-the-four-keys-to-measure-your-devops-performance',
    docLabel: 'DORA four keys metrics',
  },
  'notifications': {
    group: 'observability',
    name: 'Notifications',
    short: 'Deliver pipeline events to the right people at the right time',
    color: '#7c3aed',
    tag: 'OBSERVABILITY',
    intro: 'Notification integrations deliver pipeline events — build failures, approval requests, deployment completions — to Slack, PagerDuty, email, or Microsoft Teams. The goal is signal without noise: only alert on actionable events, route them to the right channel, and provide enough context (which branch, which step failed, link to logs) that the recipient can act immediately without digging. A broken CI system that nobody knows about is worse than a system with no CI at all.',
    deepDive: 'Notification fatigue is a real problem. A team that gets 50 Slack messages a day from CI learns to ignore them all — including the critical failures. Best practices: only notify on main branch failures (feature branch failures are the author\'s problem), always include a direct link to the failed step, tag the commit author, and use thread replies for subsequent failures of the same job rather than new top-level messages. For production deploys, a structured deploy notification in a dedicated #deployments channel serves as a real-time changelog and incident correlation aid.',
    example: {
      scenario: 'A main branch build fails at 2am. The on-call engineer needs to know immediately.',
      action: 'The workflow sends a Slack message to #incidents with the failure details and pages PagerDuty if it is during an active on-call window.',
      code: '- name: Notify on failure\n  if: failure() && github.ref == "refs/heads/main"\n  uses: slackapi/slack-github-action@v1\n  with:\n    channel-id: "C01234ABCDE"\n    payload: |\n      {\n        "text": ":red_circle: Build failed on main",\n        "blocks": [{\n          "type": "section",\n          "text": {\n            "type": "mrkdwn",\n            "text": "*<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View run>*\\nCommit: ${{ github.sha }}\\nAuthor: ${{ github.actor }}"\n          }\n        }]\n      }\n  env:\n    SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}',
    },
    responsibilities: [
      'Notify commit authors on build failures in their PRs',
      'Alert on-call engineers on main branch failures and deployment failures',
      'Send approval requests to designated reviewers via Slack or email',
      'Post a deployment record to a shared channel after every production release',
    ],
    docUrl: 'https://github.com/slackapi/slack-github-action',
    docLabel: 'Slack GitHub Action',
  },
  'deployment-slo': {
    group: 'observability',
    name: 'Deployment SLO',
    short: 'Monitor post-deploy error rate and latency to trigger automatic rollback',
    color: '#7c3aed',
    tag: 'OBSERVABILITY',
    intro: 'A deployment SLO (Service Level Objective) is a threshold — say, error rate < 0.5% and p99 latency < 800ms — that a new deployment must maintain for a defined observation window before it is considered healthy. If the SLO is breached within that window, the deployment is automatically rolled back. This turns deployment risk from a binary outcome (it works or it doesn\'t) into a quantified, observable process with a built-in safety net.',
    deepDive: 'Argo Rollouts AnalysisTemplates query Prometheus metrics after each canary step and fail the rollout if thresholds are breached. The key design decision is the observation window: too short (1 minute) and you miss slow regressions; too long (30 minutes) and rollbacks are delayed. Most teams use 5–10 minutes with a canary at 10% traffic, giving enough signal without exposing too many users. Error budget burn rate (how fast you\'re spending your SLO budget) is a more sophisticated rollback trigger than raw error rate — it accounts for baseline error rates and traffic spikes.',
    example: {
      scenario: 'A new payment service version is deployed as a 10% canary. After 3 minutes, payment errors spike from 0.1% to 2%.',
      action: 'The Argo Rollouts AnalysisTemplate detects the SLO breach, aborts the rollout, and rolls traffic back to the previous version automatically.',
      code: 'apiVersion: argoproj.io/v1alpha1\nkind: AnalysisTemplate\nmetadata:\n  name: success-rate\nspec:\n  metrics:\n  - name: success-rate\n    interval: 1m\n    successCondition: result[0] >= 0.995\n    failureLimit: 2\n    provider:\n      prometheus:\n        address: http://prometheus:9090\n        query: |\n          sum(rate(\n            http_requests_total{status!~"5..",app="api"}[1m]\n          )) /\n          sum(rate(\n            http_requests_total{app="api"}[1m]\n          ))',
    },
    responsibilities: [
      'Define error rate and latency SLO thresholds per service',
      'Query Prometheus/Datadog/CloudWatch metrics after each deploy step',
      'Trigger automatic rollback when SLO thresholds are breached',
      'Record SLO compliance per deployment for trend analysis and error budget tracking',
    ],
    docUrl: 'https://argo-rollouts.readthedocs.io/en/stable/features/analysis/',
    docLabel: 'Argo Rollouts analysis',
  },
};

export const CICD_GROUPS = [
  {
    id: 'source-control',
    title: 'Source Control',
    color: '#f97316',
    colorDark: '#c2410c',
    gradient: 'linear-gradient(135deg, #ffedd5 0%, #fed7aa 100%)',
    bg: '#fff7ed',
    components: ['dev-push', 'git-repo', 'webhook'],
  },
  {
    id: 'build-test',
    title: 'Build & Test',
    color: '#2563eb',
    colorDark: '#1e3a8a',
    gradient: 'linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%)',
    bg: '#eff6ff',
    components: ['ci-runner', 'build-stage', 'test-stage'],
  },
  {
    id: 'release',
    title: 'Release Pipeline',
    color: '#059669',
    colorDark: '#064e3b',
    gradient: 'linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%)',
    bg: '#f0fdf4',
    components: ['artifact-registry', 'staging-deploy', 'approval-gate', 'prod-deploy'],
  },
];

export const CICD_GROUP_COLOR_MAP = {
  'dev-push':          '#f97316',
  'git-repo':          '#f97316',
  'webhook':           '#f97316',
  'ci-runner':         '#2563eb',
  'build-stage':       '#2563eb',
  'test-stage':        '#2563eb',
  'artifact-registry': '#059669',
  'staging-deploy':    '#059669',
  'approval-gate':     '#059669',
  'prod-deploy':       '#059669',
};

export const CICD_SECONDARY_SECTIONS = [
  { id: 'security',    title: 'Security & Compliance', color: '#dc2626', components: ['secret-scanning', 'sast', 'dependency-audit'] },
  { id: 'observability', title: 'Pipeline Observability', color: '#7c3aed', components: ['pipeline-metrics', 'notifications', 'deployment-slo'] },
];

export const CICD_FLOWS = [
  ['dev-push',          'git-repo'],
  ['git-repo',          'webhook'],
  ['webhook',           'ci-runner'],
  ['ci-runner',         'build-stage'],
  ['build-stage',       'test-stage'],
  ['test-stage',        'artifact-registry'],
  ['artifact-registry', 'staging-deploy'],
  ['staging-deploy',    'approval-gate'],
  ['approval-gate',     'prod-deploy'],
];

export const CICD_JOURNEY = [
  { title: 'git push / PR merge',    desc: 'Developer pushes code or merges a pull request — the human event that starts everything.', actor: 'developer' },
  { title: 'Webhook fires',          desc: 'The Git provider sends an HMAC-signed HTTP POST to the CI system with the event payload.', actor: 'webhook' },
  { title: 'Runner picks up job',    desc: 'A CI runner claims the queued job, clones the repo at the target SHA, and prepares the environment.', actor: 'runner' },
  { title: 'Build executes',         desc: 'Source is compiled, dependencies resolved, and a Docker image is built and tagged with the commit SHA.', actor: 'build' },
  { title: 'Tests gate the release', desc: 'Unit, integration, SAST, and dependency CVE checks run. Any failure blocks the pipeline cold.', actor: 'test' },
  { title: 'Artifact pushed',        desc: 'The passing image is pushed to the artifact registry with an immutable SHA tag and a vulnerability scan.', actor: 'registry' },
  { title: 'Staging deploy',         desc: 'The artifact auto-deploys to staging. Smoke tests verify liveness and correctness.', actor: 'staging' },
  { title: 'Approval requested',     desc: 'Reviewers are notified. A human approves or rejects the production deployment with a reason logged.', actor: 'approval' },
  { title: 'Production release',     desc: 'The approved artifact rolls out via canary or rolling strategy, with SLO-gated automatic rollback if needed.', actor: 'production' },
];

export const CICD_TOPIC = {
    id: 'cicd',
    name: 'CI/CD',
    short: 'Build, test, release, and deploy with confidence.',
    accent: '#2563eb',
    accentSoft: '#dbeafe',
    eyebrow: 'CloudTruck · CI/CD · from git push to production',
    heroTitle: 'From git push to production, revealed.',
    heroLead: 'Trace the full pipeline lifecycle: source events, runner execution, build and test stages, artifact storage, staging validation, approval gates, and production deployment with automatic rollback. Click any component to learn what it does — from first principles to production patterns.',
    heroStats: ['16 components', 'approval gates & rollback', 'DORA metrics'],
    diagramLabel: 'PIPELINE · main · build-to-deploy graph',
    panelHint: 'trace the release flow ->',
    sectionsTitle: 'Security, compliance, and observability',
    sectionsLead: 'A production CI/CD pipeline is more than build and deploy. Secret scanning, SAST, dependency audits, DORA metrics, and deployment SLOs are the invisible scaffold that keeps every release safe and measurable.',
    workflowTitle: 'The journey of a git push',
    workflowLead: 'Follow a code change from developer keyboard to live production, through every automated gate and human checkpoint.',
    workflowCommand: 'git push origin main',
    workflowRealtimeLabel: 'Live pipeline trace',
    compareTitle: 'The mental model',
    compareParagraphs: [
      'CI/CD is event-driven automation organized into stages. Each stage is a gate: if it fails, nothing downstream runs. This makes the pipeline a quality escalator — only artifacts that have passed every prior check can reach production.',
      'The separation between CI (build and test) and CD (deploy) is not just organizational. CI produces an immutable artifact; CD delivers that artifact to environments in controlled ways. "Deploy on every merge" and "deploy on approval" can coexist in the same system because the artifact is always the same object, regardless of when it ships.',
    ],
    footerTicker: 'CI/CD · BUILD · TEST · ARTIFACT · STAGING · APPROVAL · PRODUCTION',
    emptySearch: 'No CI/CD component matched that search.',
    comingSoonCopy: 'This future topic will get the same interactive treatment.',
    components: CICD_COMPONENTS,
    groups: CICD_GROUPS,
    groupColorMap: CICD_GROUP_COLOR_MAP,
    secondarySections: CICD_SECONDARY_SECTIONS,
    flows: CICD_FLOWS,
    journey: CICD_JOURNEY,
};

export default CICD_TOPIC;
